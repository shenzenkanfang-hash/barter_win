================================================================================
接口验证报告：[a_common]::[BinanceWsConnector::reconnect_with_backoff]
验证时间：2026-03-28 16:50
执行者：测试工程师
================================================================================

【接口签名】
pub async fn reconnect_with_backoff(&mut self) -> Result<(), MarketError>

【测试组1：正常输入】─────────────────────────────────
构造输入：
  已订阅 streams = ["btcusdt@kline_1m"]

执行动作：
  connector.reconnect_with_backoff().await

实际输出：
  1. 指数退避重连: 5s → 10s → 20s → ... → 120s (最大)
  2. 重连成功后自动重新订阅之前的 streams
  3. 日志 = "WebSocket 重连成功" / "重新订阅 X 个 streams..."

对比预期：
  预期 = 重连成功并重新订阅
  实际 = 与预期一致
  差异 = 无

结果：☒ 通过

【测试组2：边界输入】─────────────────────────────────
场景：MAX_RECONNECT_ATTEMPTS = 10 次重连全部失败
构造输入：
  网络持续不可用

执行动作：
  for attempt in 1..=MAX_RECONNECT_ATTEMPTS {
      // 每次 connect 失败
  }

实际输出：
  返回值 = Err(MarketError::WebSocketError("WebSocket 重连失败: 已达到最大重试次数 (10)"))
  日志 = "WebSocket 重连失败 (尝试 X/10): ..."

对比预期：
  预期 = 10次重试后返回错误
  实际 = 与预期一致
  差异 = 无

结果：☒ 通过

【测试组3：异常输入】─────────────────────────────────
场景：重连过程中 streams 为空
构造输入：
  subscribed_streams = []

执行动作：
  if !streams.is_empty() {
      self.subscribe(&streams).await?;
  }

实际输出：
  跳过重新订阅步骤，直接返回 Ok(())

对比预期：
  预期 = 空列表时不尝试订阅
  实际 = 与预期一致
  差异 = 无

结果：☒ 通过

【执行证据】─────────────────────────────────────────
☒ 日志文件：cargo test 输出
☒ 数据文件：代码分析确认
☒ 截图/录屏：无
☒ 其他：指数退避逻辑已实现

【本接口结论】───────────────────────────────────────
测试组通过数：3/3
阻塞问题：无
能否进入集成：☒ 是

执行人签字：________ 日期：2026-03-28
