# 集成测试组 - 模块间集成验证

## 负责测试点

【集成场景1】实时行情订阅 -> 数据存储 -> 策略读取

| 序号 | 测试场景 | 验证点 | 优先级 | 状态 |
|------|---------|--------|--------|------|
| INT-001 | 1m K线订阅流程 | WS消息 -> Store写入 -> Store读取一致性 | P0 | 待测 |
| INT-002 | 1d K线订阅流程 | WS消息 -> Store写入 -> Store读取一致性 | P0 | 待测 |
| INT-003 | 订单簿订阅流程 | Depth更新 -> Store写入 -> 波动率更新 | P1 | 待测 |
| INT-004 | 多流并发 | 1m + 1d + Depth 同时订阅数据一致性 | P1 | 待测 |
| INT-005 | 分片订阅 | 100+交易对分片订阅完整性 | P1 | 待测 |

【集成场景2】账户数据同步 -> 本地状态 -> 风控检查

| 序号 | 测试场景 | 验证点 | 优先级 | 状态 |
|------|---------|--------|--------|------|
| INT-006 | 账户同步 | 账户数据 -> AccountPool -> 余额计算 | P0 | 待测 |
| INT-007 | 持仓同步 | 持仓数据 -> LocalPositionManager -> 持仓状态 | P0 | 待测 |
| INT-008 | 风控前置检查 | RiskPreChecker -> 订单预检 -> 拒绝/放行 | P0 | 待测 |
| INT-009 | 风控复检 | RiskReChecker -> 订单复检 -> 状态更新 | P1 | 待测 |
| INT-010 | 杠杆修改 | 修改杠杆 -> 风险计算 -> 持仓变化 | P1 | 待测 |

【集成场景3】策略信号 -> 检查层 -> 执行层

| 序号 | 测试场景 | 验证点 | 优先级 | 状态 |
|------|---------|--------|--------|------|
| INT-011 | 15分钟策略执行 | Signal -> CheckTable -> Trader -> Executor | P0 | 待测 |
| INT-012 | 日线策略执行 | Signal -> CheckTable -> l_1d -> 执行 | P1 | 待测 |
| INT-013 | 信号转换 | StrategySignal -> CheckSignal -> OrderRequest | P0 | 待测 |
| INT-014 | 数量计算 | QuantityCalculator -> 开仓/加仓/平仓数量 | P1 | 待测 |
| INT-015 | 状态管理 | h_15m::Status -> 持仓状态持久化 | P1 | 待测 |

【集成场景4】引擎调度 -> 事件总线 -> 模块协同

| 序号 | 测试场景 | 验证点 | 优先级 | 状态 |
|------|---------|--------|--------|------|
| INT-016 | 引擎启动 | EventEngine -> 初始化 -> 运行状态 | P0 | 待测 |
| INT-017 | 事件发布 | EventBus -> 发布Tick/Kline事件 -> 各模块接收 | P0 | 待测 |
| INT-018 | 引擎关闭 | 优雅关闭 -> 未完成订单处理 -> 状态保存 | P1 | 待测 |
| INT-019 | 错误传播 | 模块错误 -> EventBus -> 引擎错误处理 | P1 | 待测 |

【集成场景5】回测模式 (Mock环境)

| 序号 | 测试场景 | 验证点 | 优先级 | 状态 |
|------|---------|--------|--------|------|
| INT-020 | MockApiGateway | 模拟账户/持仓/交易 | P0 | 待测 |
| INT-021 | 历史数据回放 | ReplaySource -> 模拟WS -> 数据注入 | P0 | 待测 |
| INT-022 | 模拟K线生成 | KlineGenerator -> 模拟流 -> 策略处理 | P0 | 待测 |
| INT-023 | 模拟订单执行 | MockAccount -> 订单响应 -> 持仓更新 | P0 | 待测 |
| INT-024 | 波动率模拟 | 波动率数据注入 -> 策略计算 | P1 | 待测 |

【集成场景6】灾难恢复

| 序号 | 测试场景 | 验证点 | 优先级 | 状态 |
|------|---------|--------|--------|------|
| INT-025 | 启动恢复 | MemoryDiskRecovery -> 数据校验 -> 状态恢复 | P1 | 待测 |
| INT-026 | SQLite恢复 | SqliteRecoverySource -> 查询 -> 重建 | P2 | 待测 |
| INT-027 | 数据一致性 | 恢复后账户/持仓与断点前一致 | P1 | 待测 |
| INT-028 | 灾难恢复演练 | 模拟断电 -> 重启 -> 验证完整性 | P1 | 待测 |

## 前置依赖
所有模块独立测试必须通过才能开始集成测试

## 执行日志存放路径
testbench/并行测试/执行日志/集成测试_*.log

## 测试报告输出路径
testbench/并行测试/集成测试/

## 开始时间: ________
## 预计完成时间: ________
## 实际完成时间: ________
## 执行人签字: ________
