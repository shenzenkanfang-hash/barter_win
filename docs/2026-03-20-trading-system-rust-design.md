================================================================================
交易系统 Rust 重构设计文档
================================================================================
创建时间: 2026-03-20
来源: Barter-rs 架构参考 + Go 项目迁移需求讨论

================================================================================
一、项目概述
================================================================================

将现有 Go 量化交易系统迁移到 Rust，采用 Barter-rs 风格架构。

核心目标:
- 支持多周期策略并行运行（日线 + 分钟级）
- 高波动时自动切换到事件驱动高速模式
- 混合持仓模式: 资金池共享，策略持仓独立计算
- 高性能: 事件驱动 + 增量计算，非轮询

================================================================================
二、核心需求
================================================================================

1. 多周期并行
   - 日线策略: 低频，事件驱动
   - 分钟策略: 中频，事件驱动

2. 动态策略切换
   - 根据市场波动率自动启用/禁用策略
   - 高波动品种进入高速事件驱动模式
   - 低波动品种降级到普通事件模式

3. 混合持仓模式
   - 资金池: 所有策略共享同一个账户资金
   - 持仓结算: 每个策略独立计算持仓，互不干扰
   - 策略间无持仓耦合

4. 高性能要求
   - 1-2 个高波动品种同时处于高速事件驱动
   - 关键事件触发时增量计算指标
   - 同步更新，无锁路径追求低延迟

================================================================================
三、架构设计
================================================================================

采用五层架构（类比游戏引擎）：

核心概念：
- 策略 = 设计图/蓝图（名词）
- 引擎 = 执行运行时（动词）

    ┌─────────────────────────────────────────────────────────────┐
    │                      a_common                              │
    │               工具层: API/WS通用组件                      │
    └─────────────────────────────────────────────────────────────┘
                              │
    ┌─────────────────────────────────────────────────────────────┐
    │                    b_data_source                          │
    │           数据/网关层: 纯粹调用，无业务逻辑                │
    │           - exchange/: 纯粹下单 API                       │
    │           - api/: Binance REST API                       │
    │           - ws/: Binance WebSocket                       │
    └─────────────────────────────────────────────────────────────┘
                              │
    ┌─────────────────────────────────────────────────────────────┐
    │                   c_data_process                          │
    │           策略蓝图层: 指标计算、信号生成器                 │
    │           - min/: 分钟级最终信号状态机                    │
    │           - day/: 日线级最终信号状态机                    │
    │           - real_time/: 实时指标                          │
    │           - trend/: 趋势指标                              │
    └─────────────────────────────────────────────────────────────┘
                              │
    ┌─────────────────────────────────────────────────────────────┐
    │                   d_risk_monitor                          │
    │              合规约束层: 交易所硬性规则                   │
    │           - 风控预检（锁外执行）                         │
    │           - 风控复核（锁内执行）                         │
    │           - AccountPool、StrategyPool                    │
    └─────────────────────────────────────────────────────────────┘
                              │
    ┌─────────────────────────────────────────────────────────────┐
    │                      f_engine                             │
    │              引擎运行时层: 协调执行                       │
    │           - TradingEngine 主循环                          │
    │           - CheckTable 汇总判断                           │
    │           - 持久化、灾备恢复                             │
    └─────────────────────────────────────────────────────────────┘

================================================================================
四、各层职责
================================================================================

1. a_common (工具层)
   - 通用组件: API、WS、错误类型
   - 无业务逻辑依赖
   - 纯粹的工具箱

2. b_data_source (数据/网关层)
   - 纯粹调用交易所 API，无业务逻辑
   - exchange/: 纯粹下单 API
   - api/: Binance REST API
   - ws/: Binance WebSocket

3. c_data_process (策略蓝图层)
   - 指标计算: EMA、RSI、Pine颜色、价格位置
   - 信号生成: min/ (分钟级)、day/ (日线级)
   - 最终信号状态机: TradingTrigger
   - 纯设计图，不执行下单

4. d_risk_monitor (合规约束层)
   - 风控预检（锁外执行）
   - 风控复核（锁内执行）
   - AccountPool、StrategyPool
   - 硬性规则校验

5. f_engine (引擎运行时层)
   - TradingEngine 主循环
   - CheckTable 汇总判断
   - 持久化、灾备恢复
   - 协调执行

================================================================================
五、并发模型
================================================================================

核心设计原则: 事件驱动无锁

1. 主事件循环 (Tokio 单线程)
   - 处理关键事件数据
   - 增量更新指标
   - 策略信号生成
   - 零锁开销

2. 共享状态访问 (RwLock)
   - 仅用于: 资金池、账户数据
   - 低频访问，不在关键路径

3. 策略并行
   - 每个策略独立 task
   - 通过 channel 接收事件
   - 策略间完全隔离

4. 指标增量计算
   - EMA: O(1) 更新
   - SMA: O(1) 更新
   - 订单簿: O(log N) 更新
   - K线: O(1) 更新当前K线

================================================================================
六、指标设计
================================================================================

详见配套文档: `指标逻辑.md`

核心要点:
- 事件驱动 + 单向线性步进
- 零锁设计: 增量计算在主事件循环同步完成
- 高频模式: 价格硬限制 + 指标允许延迟

================================================================================
七、动态策略切换
================================================================================

1. 模式定义

   enum TradingMode {
       Low,    // 低频模式 (日线)
       Medium, // 中频模式 (分钟)
       High,   // 高速模式 (事件驱动)
   }

2. 波动率阈值（硬性标准）

   | 策略周期 | 进入高速阈值 | 说明 |
   |---------|-------------|------|
   | 1分钟 | 波动率 >= 3% | K线幅度超过3% |
   | 15分钟 | 波动率 >= 13% | K线幅度超过13% |

   阈值说明:
   - 基于当前K线与前一根K线收盘价的比率计算
   - 达到阈值后策略切换为高频模式
   - 低于阈值时降级为低频模式

3. 高频模式特性

   - 硬性价格判断标准: 必须在价格允许范围内才能交易
   - 指标允许动态延迟: 在价格容许范围内延迟计算
   - 事件驱动: 指标作为事件驱动链的一环

4. 切换机制

   - 监控市场波动率 (基于K线变化率)
   - 波动率超过阈值 → 启用高频策略
   - 波动率低于阈值 → 降级到低频策略
   - 切换时保留仓位状态和指标状态

5. 策略注册表

   struct StrategyRegistry {
       strategies: FnvHashMap<StrategyId, Box<dyn Strategy>>,
       mode: TradingMode,
   }

================================================================================
八、锁与并发控制
================================================================================

1. 核心原则: 简化锁设计

   - 锁外预检所有风控条件，不通过直接返回失败
   - 只有下单时加全局锁
   - 其他操作不加锁，零等待
   - 不需要版本号机制

2. 下单流程

   策略信号产生
       │
       ▼
   ┌───────────────────────────┐
   │ 风控预检（锁外）          │ ← 检查所有条件
   └───────────────────────────┘
       │ 不通过
       ▼ 直接返回失败
       │ 通过
       ▼
   ┌───────────────────────────┐
   │ 加全局锁（下单专用）       │
   └───────────────────────────┘
       │
       ▼
   ┌───────────────────────────┐
   │ 执行下单                  │
   │ 更新持仓/资金             │
   └───────────────────────────┘
       │
       ▼
   ┌───────────────────────────┐
   │ 释放锁                    │
   └───────────────────────────┘

3. 锁的边界

   | 操作 | 是否加锁 | 说明 |
   |------|---------|------|
   | 行情更新/指标计算 | 否 | 高频路径，零锁 |
   | 策略信号生成 | 否 | 读当前状态 |
   | 风控预检 | 否 | 检查所有条件 |
   | 订单执行 | 是 | 全局锁 |
   | 持仓/资金更新 | 是 | 锁内原子更新 |

4. 多线程策略执行

   - 每个策略独立线程/Task
   - 策略间完全隔离，无共享状态
   - 通过 channel 接收市场数据
   - 高频模式下策略专注单品种

================================================================================
九、持仓管理
================================================================================

1. 资金池 (共享)

   struct FundPool {
       total_equity: f64,        // 总权益
       available: f64,           // 可用资金
       positions_value: f64,     // 持仓占用
   }

   访问: RwLock 保护，低频更新

2. 策略持仓 (私有)

   struct StrategyPosition {
       strategy_id: StrategyId,
       symbol: Symbol,
       side: Side,
       qty: f64,
       entry_price: f64,
   }

   每个策略独立计算，不共享锁

3. 结算流程

   订单成交 → 更新策略持仓 → 结算保证金 → 更新资金池

================================================================================
十、 crates 目录结构
================================================================================

crates/
├── a_common/               # 工具层: API/WS 通用组件
│   └── src/
│       ├── api/            # Binance REST API
│       ├── ws/             # Binance WebSocket
│       ├── exchange/       # Gateway 类型定义
│       └── error.rs        # 统一错误类型
│
├── b_data_source/          # 数据/网关层: 业务数据处理
│   └── src/
│       ├── market/         # 市场数据
│       ├── feeder/         # 数据输入
│       └── persistence/     # 数据持久化
│
├── c_data_process/          # 策略蓝图层: 指标计算、信号生成
│   └── src/
│       ├── min/           # 分钟级信号状态机
│       ├── day/            # 日线级信号状态机
│       ├── pine_indicator_full/  # Pine 指标
│       ├── volatility/     # 波动率检测
│       └── types.rs        # 类型定义
│
├── d_risk_monitor/          # 合规约束层: 风控检查
│   └── src/
│       ├── risk/          # 风控模块
│       └── position/      # 持仓管理
│
└── f_engine/                # 引擎运行时层: 协调执行
    └── src/
        ├── core/          # 核心引擎
        ├── risk/          # 风控
        ├── order/         # 订单
        ├── position/      # 持仓
        ├── persistence/   # 持久化
        ├── channel/      # 通道
        └── shared/       # 共享组件

================================================================================
十一、技术选型
================================================================================

| 组件 | 技术选择 | 原因 |
|------|---------|------|
| Runtime | Tokio | 异步 IO，任务调度成熟 |
| 状态管理 | FnvHashMap | O(1) 查找，FnvHash 更快 |
| 共享状态 | RwLock | 读多写少场景优化 |
| 指标存储 | VecDeque | 固定窗口，O(1) 队首队尾 |
| 订单簿 | BTreeMap | 排序，支持范围查询 |
| 序列化 | Serde | Rust 标准 |
| 数值计算 | Rust 原始类型 | 避免 BigDecimal 开销 |

================================================================================
十二、性能目标
================================================================================

| 场景 | 目标延迟 | 实现方式 |
|------|---------|---------|
| 事件接收 | < 1ms | Tokio 异步 |
| 指标更新 | < 0.1ms | 增量计算 O(1) |
| 策略判断 | < 0.1ms | 单线程同步 |
| 信号输出 | < 1ms | Channel 传递 |
| 总延迟 | ~5-10ms | 满足秒级交易 |

================================================================================
十三、对比现有 Go 项目
================================================================================

| 对比项 | Go 微服务 | Rust 单体 |
|--------|---------|---------|
| 架构 | 多进程 gRPC | 单进程 trait 调用 |
| 延迟 | 网络开销 1-5ms | 零网络开销 <1ms |
| 状态同步 | 多进程数据一致难 | 进程内共享简单 |
| 部署 | 多服务复杂 | 单二进制简单 |
| 指标计算 | 全量 pandas | 增量计算 O(1) |
| 维护 | 多服务协调难 | 单一代码库 |

================================================================================
十四、核心业务函数调用链
================================================================================

本文档详细描述各业务模块之间的函数调用关系，从市场数据接收到订单执行的全流程。

--------------------------------------------------------------------------------

14.1 模块 1: 市场数据层
--------------------------------------------------------------------------------

WebSocket 接收事件
    │
    ├─→ kline_1m.update(tick)
    │       │
    │       └─→ 增量更新当前 1m K线 (O(1))
    │
    ├─→ kline_1d.update(tick)
    │       │
    │       └─→ 增量更新当前 1d K线 (O(1))
    │
    └─→ orderbook.update(tick)
            │
            └─→ 增量更新订单簿 (O(log N))

--------------------------------------------------------------------------------

14.2 模块 2: 指标计算层
--------------------------------------------------------------------------------

K线更新触发
    │
    ├─→ ema_12_26.calculate(close_price)      // O(1)
    ├─→ ema_20_50.calculate(close_price)       // O(1)
    └─→ ema_100_200.calculate(close_price)     // O(1)
            │
            ├─→ macd.calculate(ema_fast, ema_slow)
            │       │
            │       ├─→ signal_line.calculate(macd)   // O(1)
            │       └─→ hist = macd - signal           // O(1)
            │
            ├─→ rsi.calculate()                       // O(1)
            │
            ├─→ atr.calculate(high, low, close)      // O(1)
            │
            ├─→ tr_ratio.calculate()
            │       │   // tr_ratio_5d_20d = ATR_5d / ATR_20d
            │       │   // tr_ratio_60min_5h = ATR_60min / ATR_5h
            │
            ├─→ zscore.calculate()
            │       │   // zscore_14_1m = (price - mean_14) / std_14
            │
            └─→ pine_color.detect(macd, signal, hist, rsi)
                    │
                    ├─→ MACD >= Signal && MACD >= 0 → 纯绿
                    ├─→ MACD <= Signal && MACD >= 0 → 淡绿
                    ├─→ MACD <= Signal && MACD <= 0 → 纯红
                    ├─→ MACD >= Signal && MACD <= 0 → 淡红
                    └─→ RSI >= 70 || RSI <= 30 → 紫色

Pine 颜色判断逻辑（基于多周期 EMA 同步）:

| 颜色 | MACD 条件 | RSI 条件 | 说明 |
|------|----------|----------|------|
| 纯绿 | MACD >= Signal && MACD >= 0 | - | 强势多头 |
| 淡绿 | MACD <= Signal && MACD >= 0 | - | 弱势多头 |
| 纯红 | MACD <= Signal && MACD <= 0 | - | 强势空头 |
| 淡红 | MACD >= Signal && MACD <= 0 | - | 弱势空头 |
| 紫色 | - | RSI >= 70 或 RSI <= 30 | 极值区域 |

EMA 周期组:

| 周期组 | 快线 | 慢线 | 应用场景 |
|--------|------|------|---------|
| 12_26 | EMA12 | EMA26 | 1m 周期信号 |
| 20_50 | EMA20 | EMA50 | 辅助信号 |
| 100_200 | EMA100 | EMA200 | 1d 周期信号 |

--------------------------------------------------------------------------------

14.3 模块 3: 策略决策层
--------------------------------------------------------------------------------

每事件 / 每 K线收盘时
    │
    ├─→ VolatilityDetector.check()
    │       │
    │       ├─→ TR_ratio_60min >= 0.03 (3%) → 高波动模式
    │       └─→ TR_ratio_60min < 0.03 → 低波动模式
    │
    ├─→ 高波动模式 → MartinStrategy.check()
    │       │
    │       ├─→ PinStatusDetector.check_long_entry()
    │       │       │
    │       │       ├─→ zscore > 2
    │       │       ├─→ TR_ratio > 1
    │       │       ├─→ pos_norm > 80
    │       │       ├─→ pine_color == 纯绿
    │       │       └─→ 满足 4/7 条件 → LONG_ENTRY
    │       │
    │       ├─→ PinStatusDetector.check_short_entry()
    │       │       │ (类似逻辑，反向)
    │       │
    │       ├─→ PinStatusDetector.check_long_hedge()
    │       │       │
    │       │       ├─→ TR_ratio < 0.15
    │       │       └─→ 满足 4/6 条件 → LONG_HEDGE
    │       │
    │       └─→ PinStatusDetector.check_exit_high_vol()
    │               │
    │               └─→ TR_ratio < 0.15 + 仓位正常 → 退出高波动
    │
    └─→ 低波动模式 → TrendStrategy.check()
            │
            ├─→ TrendStatusDetector.check_long_entry()
            │       │
            │       ├─→ pine_color_all_green()    // 所有周期纯绿
            │       ├─→ TR_ratio_5d_20d > 1
            │       ├─→ ma5_close_position > 70
            │       └─→ 满足条件 → LONG_ENTRY
            │
            ├─→ TrendStatusDetector.check_short_entry()
            │       │ (类似逻辑，反向)
            │
            ├─→ TrendStatusDetector.check_long_exit()
            │       │
            │       ├─→ pine_color_max_period != 纯绿  // 最大周期非纯绿
            │       └─→ 满足条件 → LONG_EXIT
            │
            └─→ TrendStatusDetector.check_short_exit()

TrendStatusDetector 做多条件详情:
- pine 颜色: 全周期纯绿
- TR_ratio: tr_ratio_5d_20d > 1 或 tr_ratio_20d_60d > 1
- MA5 位置: ma5_close_in_all_ma5_pos > 70
- 条件满足: >= 2/3

TrendStatusDetector 做空条件详情:
- pine 颜色: 全周期紫色/纯红
- TR_ratio: 同上
- MA5 位置: ma5_close_in_all_ma5_pos < 30
- 条件满足: >= 2/3

--------------------------------------------------------------------------------

14.4 模块 4: 风控检查层
--------------------------------------------------------------------------------

策略信号产生 (LONG_ENTRY / SHORT_ENTRY / HEDGE / EXIT)
    │
    ├─→ RiskPreChecker.pre_check()     // 锁外执行
    │       │
    │       ├─→ check_account_balance()
    │       │       │ // 可用资金 >= 最低保留
    │       │
    │       ├─→ check_position_ratio()
    │       │       │ // 订单金额 / 总权益 <= 最大比例
    │       │
    │       ├─→ check_symbol_registered()
    │       │       │ // 品种已注册到该策略
    │       │
    │       └─→ check_volatility_mode()
    │               │ // 波动率模式匹配
    │
    │       └─→ 不通过 → 返回 RejectReason
    │
    └─→ 通过 → 进入下单流程

--------------------------------------------------------------------------------

14.5 模块 5: 下单执行层
--------------------------------------------------------------------------------

PreCheck 通过
    │
    ├─→ GlobalLock.acquire()          // 获取全局锁
    │       └─→ 失败 → 返回 LockFailed
    │
    ├─→ RiskReChecker.re_check()      // 锁内再次风控 (可选)
    │
    ├─→ OrderService.place_order()
    │       │
    │       ├─→ 市价单
    │       │       └─→ gateway.place_market_order()
    │       │
    │       └─→ 限价单
    │               └─→ gateway.place_limit_order()
    │
    ├─→ 等待成交回报
    │       │
    │       └─→ OrderResult { status: FILLED / REJECTED / CANCELLED }
    │
    └─→ GlobalLock.release()           // 释放全局锁

说明:
- 限价单挂单时不冻结保证金，成交后才占用
- 不需要预占用机制
- 下单失败不重试，直接返回

--------------------------------------------------------------------------------

14.6 模块 6: 持仓 / 资金更新层
--------------------------------------------------------------------------------

成交回报到达
    │
    ├─→ PositionManager.update_local()
    │       │
    │       ├─→ 开多: LongQty += qty, LongAvgPrice 更新
    │       ├─→ 开空: ShortQty += qty, ShortAvgPrice 更新
    │       ├─→ 平多: LongQty -= qty, 计算已实现盈亏
    │       └─→ 平空: ShortQty -= qty, 计算已实现盈亏
    │
    ├─→ FundPool.update()
    │       │
    │       ├─→ 盈利时: CurrentBalance += pnl
    │       └─→ 亏损时: CurrentBalance -= pnl
    │
    ├─→ StrategyPosition.update()      // 策略私有持仓
    │       │
    │       └─→ 独立计算该策略的持仓和盈亏
    │
    └─→ PersistenceService.save()
            │
            ├─→ SQLite: 交易记录、持仓快照
            └─→ /dev/shm: K线备份、缓存

--------------------------------------------------------------------------------

14.7 模块 7: 策略状态机
--------------------------------------------------------------------------------

马丁格尔策略状态流转:

    ┌─────────────────────────────────────────────────────────────┐
    │                         IDLE                                │
    │                    (等待入场)                               │
    └─────────────────────────┬───────────────────────────────────┘
                              │
                              │ check_long_entry() 或 check_short_entry()
                              ▼
    ┌─────────────────────────────────────────────────────────────┐
    │                        OPENING                              │
    │                   (马丁格尔加仓中)                           │
    └───────┬─────────────────────┬───────────────────────────────┘
            │                     │
            │ check_long_hedge() │ check_long_exit()
            ▼                     ▼
    ┌───────────────┐     ┌───────────────┐
    │   HEDGING      │     │   CLOSING     │
    │   (对冲中)     │     │   (平仓中)    │
    └───────┬───────┘     └───────┬───────┘
            │                     │
            │ 对冲盈利回落 1%      │ 持仓清零
            ▼                     ▼
    ┌───────────────┐     ┌───────────────┐     check_exit_high_vol()
    │  CLOSING_ALL  │     │     IDLE      │◄────────────────────┐
    │   (全平)       │     │               │                      │
    └───────┬───────┘     └───────────────┘                      │
            │                                                       │
            │ 趋势确认                                              │
            └──────────────────────┐                                │
                                   │                                │
                                   ▼                                │
                          ┌─────────────────┐                      │
                          │   TREND_MODE    │──────────────────────┘
                          │  (趋势跟踪模式)   │    趋势结束
                          └─────────────────┘

状态说明:

| 状态 | 说明 | 触发条件 |
|------|------|---------|
| IDLE | 等待入场 | 无持仓，等待信号 |
| OPENING | 马丁加仓中 | 入场后，每次加仓条件满足 |
| HEDGING | 对冲中 | 多单回落时做空对冲 |
| CLOSING | 平仓中 | 达到平仓条件 |
| CLOSING_ALL | 全平 | 对冲盈利回落 1% |
| TREND_MODE | 趋势跟踪 | 高波动结束，进入趋势 |

--------------------------------------------------------------------------------

14.8 模块 8: 指标周期对应关系
--------------------------------------------------------------------------------

| 指标 | 1m 周期 | 1d 周期 |
|------|---------|---------|
| EMA 组 | 12_26 | 100_200 |
| Pine 颜色 | 12_26 | 100_200 |
| TR Ratio | 60min/5h, 10min/1h | 5d/20d, 20d/60d |
| Z-Score | 14_1m, 1h_1m | - |
| RSI | 14 | - |

--------------------------------------------------------------------------------

14.9 完整交易流程总图
--------------------------------------------------------------------------------

    ┌─────────────────────────────────────────────────────────────────────────────┐
    │                        市场数据层 (Market Data)                           │
    │  WebSocket ──► 事件 ──► K线合成 ──► 指标计算 ──► Pine颜色              │
    └─────────────────────────────────────────────────────────────────────────────┘
                                          │
                                          ▼
    ┌─────────────────────────────────────────────────────────────────────────────┐
    │                        策略决策层 (Strategy)                                │
    │  VolatilityDetector ──► [高波动?]                                          │
    │         │                    │                                              │
    │         ├─ 是 ──► MartinStrategy ──► PinStatusDetector                     │
    │         │                                                              │
    │         └─ 否 ──► TrendStrategy ──► TrendStatusDetector                    │
    └─────────────────────────────────────────────────────────────────────────────┘
                                          │
                                          ▼ 信号产生
    ┌─────────────────────────────────────────────────────────────────────────────┐
    │                        风控检查层 (Risk Check)                              │
    │  RiskPreChecker.pre_check() ──► [通过?]                                   │
    │         │                        │                                          │
    │         ├─ 否 ──► 返回 Reject                                                   │
    │         └─ 是 ──► 进入下单流程                                                │
    └─────────────────────────────────────────────────────────────────────────────┘
                                          │
                                          ▼
    ┌─────────────────────────────────────────────────────────────────────────────┐
    │                        下单执行层 (Order Execution)                          │
    │  GlobalLock.acquire() ──► OrderService.place_order() ──► 成交回报           │
    │         │                        │                                          │
    │         └─► GlobalLock.release() ◄──────────────────────────────────────────┘
    └─────────────────────────────────────────────────────────────────────────────┘
                                          │
                                          ▼
    ┌─────────────────────────────────────────────────────────────────────────────┐
    │                        持仓/资金层 (Position/Fund)                           │
    │  PositionManager.update() ──► FundPool.update() ──► PersistenceService.save()│
    └─────────────────────────────────────────────────────────────────────────────┘

================================================================================
十五、参考项目
================================================================================

15.1 Rust 交易框架
--------------------------------------------------------------------------------

| 项目 | GitHub | 特点 | 借鉴价值 |
|------|--------|------|---------|
| barter-rs | ztNozdormu/barter-rs | 事件驱动、多周期支持 | 高 - 主要架构参考 |
| Botvana | 高性能事件驱动 | 交易系统 | 中 - 架构参考 |
| FXyou | apssouza22/trading-system | Java 高频交易 | 中 - 架构模式 |

15.2 Rust 订单簿实现
--------------------------------------------------------------------------------

| 项目 | GitHub | 特点 | 借鉴价值 |
|------|--------|------|---------|
| orderbook-rs | GitHub HFT topic | 线程安全、无锁设计 | 高 - 数据结构参考 |
| limit-order-book | 限价订单簿 | 匹配引擎 | 中 - 算法参考 |

15.3 Rust 其他相关
--------------------------------------------------------------------------------

| 项目 | 用途 | 借鉴价值 |
|------|------|---------|
| rust-ml/linfa | 机器学习框架 | 中 - 策略分析 |
| Vector (timberio) | 高性能数据管道 | 中 - 流处理架构 |
| rust-in-blockchain/awesome | 区块链/加密货币 | 中 - 交易所集成 |

15.4 架构共同思想
--------------------------------------------------------------------------------

从多个参考项目中提炼的共识架构模式:

1. **事件驱动** - 所有交易系统都采用事件驱动架构
2. **无锁高频路径** - 事件处理无锁是高性能共识
3. **Trait 抽象** - 可插拔组件是标准设计模式
4. **Actor 风格** - 状态封装 + 消息传递

15.5 需要避免的问题
--------------------------------------------------------------------------------

参考项目中发现的常见问题:

| 问题 | 来源 | 后果 | 我们的方案 |
|------|------|------|-----------|
| panic!() 错误处理 | barter-rs | 解析错误导致崩溃 | 全部使用 Result |
| RwLock 竞争 | barter-rs | 高频路径性能差 | 无锁事件处理 |
| 过多 clone() | barter-rs (344处) | 内存压力 | 优先引用 + Arc |
| 测试不足 | barter-rs (1个集成测试) | 重构风险高 | 全面测试覆盖 |

================================================================================
十六、流水线并行架构
================================================================================

本文档描述基于品种级并行的流水线架构设计，支持多策略 x 多品种的灵活组合。

--------------------------------------------------------------------------------
16.1 核心设计理念
--------------------------------------------------------------------------------

品种间依赖解耦:
- 每个品种作为独立流水线，互不阻塞
- 策略配置化，不硬编码
- 统一 Check 表记录各层结果
- 最后汇聚判断仓位操作（互斥）

架构原则:
- 慢速通道: 时间驱动（分钟级），同步判断
- 高速通道: 独立通道，高频计算
- 下单串行: 获锁 + 风控复核

--------------------------------------------------------------------------------
16.2 流水线架构图
--------------------------------------------------------------------------------

    ┌─────────────────────────────────────────────────────────────────────────┐
    │                     品种流水线 (每品种一条，互不阻塞)                     │
    │                                                                       │
    │  ┌─────────┐    ┌─────────┐    ┌─────────┐    ┌─────────┐            │
    │  │ 市场数据 │ -> │ 指标计算 │ -> │ 策略判断 │ -> │ 填Check表│            │
    │  │ (Tick)  │    │ (EMA等) │    │ (异步)  │    │ (结果)  │            │
    │  └─────────┘    └─────────┘    └─────────┘    └─────────┘            │
    │       │              │              │              │                  │
    │       └──────────────┴──────────────┴──────────────┘                  │
    │                            │                                          │
    └────────────────────────────┼──────────────────────────────────────────┘
                                 │
                                 ▼
    ┌─────────────────────────────────────────────────────────────────────────┐
    │                    Check 表 (统一判断层)                               │
    │                                                                       │
    │  品种ID │ 策略ID │ EMA信号 │ RSI信号 │ Pine颜色 │ 最终信号 │ 风险标记  │
    │  ───────┼────────┼─────────┼─────────┼──────────┼──────────┼─────────  │
    │  BTC    │ 趋势   │   多    │  偏多   │   纯绿   │   做多   │   -      │
    │  ETH    │ 趋势   │   空    │  偏空   │   纯红   │   做空   │   -      │
    │  SOL    │ 马丁   │   多    │  超买   │   淡绿   │   观望   │  高波动  │
    │                                                                       │
    └─────────────────────────────────────────────────────────────────────────┘
                                 │
                                 ▼
    ┌─────────────────────────────────────────────────────────────────────────┐
    │                    仓位决策层 (互斥判断)                                │
    │                                                                       │
    │  - 汇总所有品种的 Check 表                                              │
    │  - 判断哪些品种可以操作（互斥）                                         │
    │  - 产生 OrderRequest 列表                                               │
    └─────────────────────────────────────────────────────────────────────────┘
                                 │
                                 ▼
    ┌─────────────────────────────────────────────────────────────────────────┐
    │                    风控预检层 (锁外)                                    │
    │                                                                       │
    │  1. 账户资金检查 (可用 >= 最低保留)                                     │
    │  2. 持仓比例检查 (订单金额/总权益 <= 最大比例)                          │
    │  3. 品种注册检查 (品种已注册到该策略)                                  │
    │  4. 波动率模式检查 (与当前模式匹配)                                    │
    │                                                                       │
    │  不通过 -> 标记风险，跳过该品种                                        │
    │  通过 -> 进入下单执行                                                  │
    └─────────────────────────────────────────────────────────────────────────┘
                                 │
                                 ▼
    ┌─────────────────────────────────────────────────────────────────────────┐
    │                    下单执行层 (串行 + 锁)                               │
    │                                                                       │
    │  GlobalLock.acquire() -> 锁内风控复核 -> 下单 -> 释放锁               │
    │                                                                       │
    └─────────────────────────────────────────────────────────────────────────┘

--------------------------------------------------------------------------------
16.3 双通道设计
--------------------------------------------------------------------------------

高速通道 vs 慢速通道:

| 特性         │ 高速通道              │ 慢速通道              │
|──────────────┼──────────────────────┼──────────────────────│
| 触发条件     │ 1min波动率 >= 3%     │ 时间驱动（每分钟）    │
│              │ 15min波动率 >= 13%   │                      │
| 计算频率     │ 事件驱动，完全独立判断  │ K线收盘时             │
| 判断方式     │ 事件驱动，独立出结果   │ 统一时间点汇总        │
| 汇入 Check 表│ 实时                  │ 统一时间点            │
| 轮次         │ 独立轮次，不混慢速    │ 统一轮次编号          │
| 通道隔离     │ 独立通道              │ 统一汇合点            │

波动率阈值（硬性标准）:


| 周期  │ 进入高速阈值 │ 计算方式                |
|-------+-------------+------------------------|
| 1分钟 │ >= 3%       │ 1m K线 (O-C) 差值      |
| 15分钟│ >= 13%      │ 15m K线 (O-C) 差值     |

说明:
- 1m波动率: 使用1分钟K线的收盘价-开盘价比率
- 15m波动率: 使用15分钟K线的收盘价-开盘价比率
- 达到阈值后，该品种进入高速通道，事件驱动独立判断
- 低于阈值后，退出高速通道，回归慢速通道

--------------------------------------------------------------------------------
16.4 Check 表结构
--------------------------------------------------------------------------------

表字段设计:

struct CheckEntry {
    symbol:         String,      // 品种ID: "BTC", "ETH"
    strategy_id:    String,      // 策略ID: "trend", "martin"
    period:         Period,      // 周期: "1m", "5m", "15m", "1d"

    // 指标信号
    ema_signal:     Signal,      // EMA 信号: Long/Short/Neutral
    rsi_value:      Decimal,      // RSI 数值: 0-100
    pine_color:     PineColor,   // Pine颜色: Green/Red/Purple/Neutral
    price_position: Decimal,      // 价格位置: 0-100 (close-low)/(high-low)

    // 最终判断
    final_signal:   Signal,      // 最终信号: Long/Short/Neutral/Watch
    target_price:   Decimal,      // 目标价格（下单用）
    quantity:       Decimal,      // 目标数量（下单用）
    risk_flag:      bool,        // 风险标记: true=需关注

    // 元数据
    timestamp:      DateTime<Utc>,
    round_id:       u64,         // 轮次ID
    is_high_freq:   bool,        // 是否高速通道
}

Check 表数据结构:

struct CheckTable {
    entries: FnvHashMap<(String, String, Period), CheckEntry>,  // key: (品种, 策略, 周期)
    round_id: u64,                      // 当前轮次ID
}

--------------------------------------------------------------------------------
16.5 一轮编码机制
--------------------------------------------------------------------------------

目的: 确保计算周期一致性，同一轮内的计算可以互相操作。

设计:

struct RoundGuard {
    round_id: AtomicU64,
    start_time: AtomicI64,
}

impl RoundGuard {
    // 开始新的一轮
    fn start_new_round(&self) -> u64 {
        self.round_id.fetch_add(1, Ordering::SeqCst)
    }

    // 检查是否同轮
    fn is_same_round(&self, round_id: u64) -> bool {
        self.round_id.load(Ordering::SeqCst) == round_id
    }
}

流程:

1. 慢速通道（时间触发，每分钟）:
   - 生成新 round_id（慢速通道独立编号）
   - 所有品种流水线开始计算
   - 填入 Check 表（记录 round_id）

2. 高速通道（事件触发，独立判断）:
   - 每次事件触发后生成独立 round_id
   - 不等待慢速通道，独立出结果
   - 高速通道结果直接触发下单流程

3. 汇合判断:
   - 慢速通道: 等待所有流水线完成，统一判断
   - 高速通道: 独立判断，直接触发风控和下单
   - 两者互不干扰，通道隔离

--------------------------------------------------------------------------------
16.6 策略模块接入方式
--------------------------------------------------------------------------------

用户策略为 Rust 模块，放在 strategies/ 目录:

strategies/
├── Cargo.toml
├── src/
│   ├── lib.rs
│   ├── trend.rs        // 趋势策略
│   ├── martin.rs       // 马丁策略
│   └── custom.rs       // 用户自定义策略

策略 trait:

pub trait Strategy: Send + Sync {
    // 策略标识
    fn id(&self) -> &str;

    // 异步判断，返回结果填入 CheckEntry
    fn check(&self, ctx: &StrategyContext) -> CheckEntry;

    // 策略-specific 风控检查
    fn risk_check(&self, order: &OrderRequest) -> Result<(), StrategyError>;
}

策略注册（配置驱动）:

// strategies.toml
[[strategies]]
id = "trend_btc_1d"
type = "trend"
symbol = "BTC"
period = "1d"
enabled = true

[[strategies]]
id = "martin_sol_1m"
type = "martin"
symbol = "SOL"
period = "1m"
enabled = true
high_freq = true  // 高频通道

--------------------------------------------------------------------------------
16.7 异步流程 vs 同步流程
--------------------------------------------------------------------------------

异步部分（可并行）:

    流水线执行 (每品种独立)
           │
           ├─> strategy.check(ctx)  // 异步，不阻塞
           │
           ├─> 填入 Check 表
           │
           └─> 返回 pending

同步部分（串行）:

    汇合判断 (固定时间点)
           │
           ├─> 等待所有流水线完成
           │
           ├─> 汇总 Check 表
           │
           └─> 统一判断仓位

    下单执行 (必须串行)
           │
           ├─> acquire global lock
           │
           ├─> risk re-check
           │
           ├─> place order
           │
           └─> release global lock

--------------------------------------------------------------------------------
16.8 仓位互斥判断
--------------------------------------------------------------------------------

互斥原则:
- 同品种 + 同策略: LONG 和 SHORT 互斥
- 同品种 + 不同策略: 不互斥，各自独立持仓
- 跨品种: 可选配置，通过配置文件控制

判断逻辑:

fn check_position_mutual_exclusion(table: &CheckTable) -> Vec<OrderRequest> {
    let mut orders = Vec::new();

    for entry in table.entries.values() {
        match entry.final_signal {
            Signal::Long => {
                // 检查同一策略是否有 SHORT（按策略分开）
                if has_short_position(&entry.symbol, &entry.strategy_id) {
                    continue;  // 同策略互斥，跳过
                }
                orders.push(create_long_order(entry));
            }
            Signal::Short => {
                // 检查同一策略是否有 LONG（按策略分开）
                if has_long_position(&entry.symbol, &entry.strategy_id) {
                    continue;  // 同策略互斥，跳过
                }
                orders.push(create_short_order(entry));
            }
            Signal::Neutral | Signal::Watch => {
                // 平仓信号（同策略的持仓）
                if has_position(&entry.symbol, &entry.strategy_id) {
                    orders.push(create_close_order(entry));
                }
            }
        }
    }

    orders
}

--------------------------------------------------------------------------------
16.9 风控检查层
--------------------------------------------------------------------------------

风控分为两层：锁外预检 + 锁内复核

16.9.1 锁外预检 (RiskPreChecker)

在获取全局锁之前执行，检查所有风控条件:

struct RiskPreChecker;

impl RiskPreChecker {
    pub fn pre_check(
        &self,
        order: &OrderRequest,
        fund_pool: &RwLockReadGuard<FundPool>,
        positions: &FnvHashMap<StrategyId, StrategyPosition>,
    ) -> Result<(), RejectReason> {
        // 1. 账户资金检查
        self.check_account_balance(order, fund_pool)?;

        // 2. 持仓比例检查
        self.check_position_ratio(order, fund_pool)?;

        // 3. 品种注册检查
        self.check_symbol_registered(order)?;

        // 4. 波动率模式检查
        self.check_volatility_mode(order)?;

        Ok(())
    }
}

风控条件详情:

| 检查项 | 条件 | 不通过原因 |
|--------|------|-----------|
| 账户资金 | 可用资金 >= 最低保留 | InsufficientBalance |
| 持仓比例 | 订单金额/总权益 <= 最大比例 | PositionRatioExceeded |
| 品种注册 | 品种已注册到该策略 | SymbolNotRegistered |
| 波动率模式 | 波动率模式与策略匹配 | VolatilityModeMismatch |

16.9.2 锁内复核 (RiskReChecker)

获取全局锁后再次核对，确保并发安全:

impl RiskReChecker {
    pub fn re_check(
        &self,
        order: &OrderRequest,
        fund_pool: &RwLockWriteGuard<FundPool>,
    ) -> Result<(), RejectReason> {
        // 再次检查资金（防止并发修改）
        if fund_pool.available < order.amount {
            return Err(RejectReason::InsufficientBalance);
        }

        // 再次检查波动率（市场可能已变化）
        self.check_volatility_realtime(order)?;

        Ok(())
    }
}

16.9.3 风控流程图

    策略信号产生 (LONG_ENTRY / SHORT_ENTRY / EXIT)
           │
           ▼
    ┌───────────────────────────┐
    │  风控预检（锁外）          │
    │  - check_account_balance  │
    │  - check_position_ratio   │
    │  - check_symbol_registered│
    │  - check_volatility_mode  │
    └───────────────────────────┘
           │ 不通过
           ▼ 返回 RejectReason
           │ 通过
           ▼
    ┌───────────────────────────┐
    │  获取全局锁 (GlobalLock)   │
    └───────────────────────────┘
           │ 获取失败
           ▼ 返回 LockFailed
           │ 获取成功
           ▼
    ┌───────────────────────────┐
    │  风控复核（锁内）          │
    │  - 再次检查资金            │
    │  - 实时波动率确认          │
    └───────────────────────────┘
           │ 不通过
           ▼ 释放锁，返回 RejectReason
           │ 通过
           ▼
    ┌───────────────────────────┐
    │  执行下单                  │
    │  - 更新持仓/资金           │
    └───────────────────────────┘
           │
           ▼
    ┌───────────────────────────┐
    │  释放锁 (GlobalLock)       │
    └───────────────────────────┘

--------------------------------------------------------------------------------
16.10 策略状态机定义
--------------------------------------------------------------------------------

本文档描述趋势策略和马丁策略的完整状态机逻辑。

--------------------------------------------------------------------------------
16.10.1 趋势策略 (TrendStrategy)
--------------------------------------------------------------------------------

**市场状态检测**:

| 市场状态 | 说明 | 检测条件 |
|---------|------|---------|
| TREND | 趋势状态 | 有明确方向 |
| RANGE | 震荡状态 | 低波动、低动能 |
| PIN | 插针状态 | 极端波动 |
| INVALID | 数据无效 | 超时/异常 |

**趋势策略状态**:

| 状态 | 说明 | 触发条件 |
|------|------|---------|
| IDLE | 等待入场 | 无持仓，等待信号 |
| LONG | 持有多头 | 做多开仓条件满足 |
| SHORT | 持有空头 | 做空开仓条件满足 |

**做多开仓条件** (2/2 条件满足):
- pine颜色: 所有有效周期都是纯绿
- extreme_vol: tr_ratio_5d_20d > 1 或 tr_ratio_20d_60d > 1
- ma5_close_in_all_ma5_pos > 70

**做空开仓条件** (2/2 条件满足):
- pine颜色: 所有有效周期都是紫色/纯红
- extreme_vol: 同上
- ma5_close_in_all_ma5_pos < 30

**做多平仓条件** (使用最大有效周期):
- ma5_close_in_all_ma5_pos > 50
- 最大周期pine颜色非纯绿

**做空平仓条件** (使用最大有效周期):
- ma5_close_in_all_ma5_pos < 50
- 最大周期pine颜色非纯红

**Pine颜色分组优先级**:
- 周期优先级: 100_200 > 20_50 > 12_26
- 开仓: 最小周期12_26优先，必须是纯绿/纯红
- 平仓: 最大有效周期优先

--------------------------------------------------------------------------------
16.10.2 马丁/插针策略 (PinStrategy)
--------------------------------------------------------------------------------

**做多开仓条件** (7条件满足≥4):
| 条件 | 指标 | 阈值 |
|------|------|------|
| extreme_z | zscore_14_1m 或 zscore_1h_1m | > 2 |
| extreme_vol | tr_ratio_60min_5h 或 tr_ratio_10min_1h | > 1 |
| extreme_pos | pos_norm_60 | > 80 |
| extreme_speed | acc_percentile_1h | > 90 |
| extreme_bg_color | pine_bg_color | == 纯绿 |
| extreme_bar_color | pine_bar_color | == 纯绿 |
| price_deviation | price_deviation_horizontal_position | == 100 |

**做空开仓条件** (7条件满足≥4):
| 条件 | 指标 | 阈值 |
|------|------|------|
| extreme_z | zscore_14_1m 或 zscore_1h_1m | > 2 |
| extreme_vol | tr_ratio_60min_5h 或 tr_ratio_10min_1h | > 1 |
| extreme_pos | pos_norm_60 | < 20 |
| extreme_speed | acc_percentile_1h | < 10 |
| extreme_bg_color | pine_bg_color | == 纯红 |
| extreme_bar_color | pine_bar_color | == 纯红 |
| price_deviation | price_deviation_horizontal_position | == 0 |

**对冲条件** (6条件满足≥4，TR<15%):
| 条件 | 指标 | 阈值 |
|------|------|------|
| extreme_vol | tr_ratio_60min_5h | > 2 或 > 1 |
| extreme_pos | pos_norm_60 | < 90 |
| extreme_speed | acc_percentile_1h 和 velocity_percentile_1h | < 10 |
| extreme_bg_color | pine_bg_color | != 纯绿 |
| extreme_bar_color | pine_bar_color | != 纯绿 |
| price_deviation | price_deviation_horizontal_position | 10 < x <= 90 |

**退出高波动条件** (3条件满足≥2，TR<15%):
| 条件 | 指标 | 阈值 |
|------|------|------|
| extreme_vol | tr_ratio_60min_5h < 1 且 tr_ratio_10min_1h < 1 | - |
| extreme_pos | 20 < pos_norm_60 < 80 | - |
| price_deviation | 10 < abs(price_deviation_horizontal_position) <= 90 | - |

**马丁格尔策略状态机**:

    IDLE ──(做多开仓)──> LONG_OPENING ──(加仓)──> LONG_HOLDING
                        │                           │
                        │                           │
                        └──(对冲)──> HEDGING <──────┘
                                                │
                        ──(盈利回落)──> CLOSING ──(全平)──> IDLE

状态说明:
| 状态 | 说明 |
|------|------|
| IDLE | 等待入场 |
| LONG_OPENING | 多头马丁加仓中 |
| SHORT_OPENING | 空头马丁加仓中 |
| LONG_HOLDING | 多头持仓中 |
| SHORT_HOLDING | 空头持仓中 |
| HEDGING | 对冲中 |
| CLOSING | 平仓中 |

--------------------------------------------------------------------------------
16.10.3 市场状态检测优先级
--------------------------------------------------------------------------------

检测顺序 (从上到下，优先级递减):
1. 数据有效性检查 (INVALID)
2. 插针检测 (PIN) - 优先级最高
3. 震荡检测 (RANGE)
4. 趋势检测 (TREND)

插针强度等级:
| 强度 | 条件数 |
|------|--------|
| WEAK | ≥2 |
| MODERATE | ≥3 |
| STRONG | ≥4 |

--------------------------------------------------------------------------------
16.10.4 持仓比例与冻结资金
--------------------------------------------------------------------------------

**持仓比例计算**:
持仓比例 = 订单金额 / 总权益

其中:
- 订单金额 = 数量 × 当前价格
- 总权益 = 可用资金 + 持仓占用

**冻结资金机制** (待确认):
- 限价单挂单时是否冻结保证金？
- 目前文档建议: 限价单挂单时不冻结，成交后才占用

--------------------------------------------------------------------------------
17.1 与原有架构的关系
--------------------------------------------------------------------------------

本文档描述的流水线架构是原有四层架构的扩展:

| 原架构层次     │ 流水线架构中的角色                        │
|---------------+-----------------------------------------│
| 市场数据层     │ 流水线输入（事件/K线）                    │
| 指标层         │ 流水线内部计算（EMA/RSI/PineColor）       │
| 策略层         │ 流水线异步判断 + Check 表记录             │
| 引擎层         │ Check 表汇合 + 仓位决策 + 下单串行执行     │

主要变化:

1. 策略层新增异步模式:
   - 策略判断从同步改为异步
   - 结果填入 Check 表

2. 引擎层新增 Check 表:
   - 统一存储各流水线结果
   - 汇合判断层

3. 新增一轮编码:
   - 保证计算周期一致性
   - 支持并行同时确保正确性

--------------------------------------------------------------------------------
17.2 全流程表单贯穿设计
--------------------------------------------------------------------------------

核心理念: 从价格数据开始，一张表单贯穿所有层级，每层携带计算结果进入下一层。

表单设计 (PipelineForm):

struct PipelineForm {
    // 基础信息
    symbol:     String,           // 品种
    strategy_id: String,          // 策略ID
    period:     Period,           // 周期
    timestamp:  DateTime<Utc>,    // 时间戳
    round_id:   u64,              // 轮次ID

    // 价格数据层产出
    event_price:     Decimal,      // 当前价格
    event_volume:    Decimal,      // 当前成交量
    open_price:     Decimal,      // K线开盘价
    close_price:    Decimal,      // K线收盘价
    high_price:     Decimal,      // K线最高价
    low_price:      Decimal,      // K线最低价

    // 指标层产出
    ema_fast:       Decimal,      // EMA快线
    ema_slow:       Decimal,      // EMA慢线
    ema_signal:     Signal,       // EMA信号
    rsi_value:      Decimal,      // RSI值
    pine_color:     PineColor,    // Pine颜色
    price_position: Decimal,      // 价格位置 0-100

    // 策略层产出
    final_signal:   Signal,       // 最终信号
    confidence:     u8,           // 置信度 0-100
    conditions_met: u8,           // 满足条件数
    conditions_total: u8,         // 总条件数

    // 下单信息
    target_price:   Decimal,      // 目标价格
    quantity:       Decimal,       // 目标数量

    // 风控标记
    risk_flag:      bool,         // 风险标记
    reject_reason:  Option<RejectReason>,  // 拒绝原因

    // 通道信息
    is_high_freq:   bool,         // 是否高速通道
}

层级处理流程:

1. 价格数据层 (MarketDataLayer)
   输入: 原始事件
   产出: PipelineForm { event_price, event_volume, open/close/high/low }
   操作: 解析事件，更新当前K线

2. 指标计算层 (IndicatorLayer)
   输入: PipelineForm (价格数据填充)
   产出: PipelineForm { ema_fast, ema_slow, rsi_value, pine_color, price_position }
   操作: 增量计算EMA/RSI/PineColor

3. 策略判断层 (StrategyLayer)
   输入: PipelineForm (指标数据填充)
   产出: PipelineForm { final_signal, confidence, target_price, quantity }
   操作: 策略逻辑判断，生成信号和下单信息

4. Check表汇总层 (CheckTableLayer)
   输入: PipelineForm (策略数据填充)
   产出: CheckTable.entries[Key] = CheckEntry
   操作: 填入Check表

5. 仓位决策层 (PositionDecisionLayer)
   输入: CheckTable
   产出: Vec<OrderRequest>
   操作: 互斥判断，决定可执行订单

6. 风控预检层 (RiskPreCheckLayer)
   输入: OrderRequest
   产出: OrderRequest 或 RejectReason
   操作: 锁外风控检查

7. 下单执行层 (OrderExecutionLayer)
   输入: OrderRequest (通过风控)
   产出: OrderResult
   操作: 获锁 → 锁内复核 → 下单 → 释放锁

全流程示意:

事件数据$
   │
   ▼
┌─────────────────────────────┐
│ 价格数据层                   │
│ 产出: price, volume, O/H/L/C │
└─────────────────────────────┘
   │ PipelineForm
   ▼
┌─────────────────────────────┐
│ 指标计算层                   │
│ 产出: EMA, RSI, Pine, Pos  │
└─────────────────────────────┘
   │ PipelineForm
   ▼
┌─────────────────────────────┐
│ 策略判断层                   │
│ 产出: Signal, Price, Qty   │
└─────────────────────────────┘
   │ PipelineForm
   ▼
┌─────────────────────────────┐
│ Check表汇总                  │
│ 产出: CheckEntry            │
└─────────────────────────────┘
   │ CheckTable
   ▼
┌─────────────────────────────┐
│ 仓位决策层                   │
│ 产出: Vec<OrderRequest>     │
└─────────────────────────────┘
   │ OrderRequest
   ▼
┌─────────────────────────────┐
│ 风控预检层                   │
│ 产出: 通过/拒绝              │
└─────────────────────────────┘
   │ OrderRequest
   ▼
┌─────────────────────────────┐
│ 下单执行层                   │
│ 产出: OrderResult           │
└─────────────────────────────┘

特点:
- 每层都是增量计算，O(1)复杂度
- 表单贯穿全程，携带所有中间结果
- 高速通道和慢速通道各自独立表单流
- 无需在不同模块间传递零散数据

--------------------------------------------------------------------------------
17.3 核心交易执行逻辑
--------------------------------------------------------------------------------

本文档描述 `singleAssetTrader` 单币种交易执行类的核心逻辑。

--------------------------------------------------------------------------------
17.3.1 核心组件
--------------------------------------------------------------------------------

| 组件 | 类型 | 说明 |
|------|------|------|
| MarketDetector | 市场检测器 | 检测 PIN/RANGE/TREND/INVALID |
| PinStatusDetector | 插针检测器 | 马丁格尔插针条件检测 |
| TrendStatusDetector | 趋势检测器 | 日线趋势条件检测 |
| RiskEngine | 风控引擎 | 订单风控校验 |
| LocalPositionManagerDecimal | 仓位管理器 | 本地持仓管理 |

--------------------------------------------------------------------------------
17.3.2 核心阈值常量
--------------------------------------------------------------------------------

| 常量 | 值 | 说明 |
|------|-----|------|
| PROFIT_THRESHOLD | 0.01 | 1%盈利平仓阈值 |
| PRICE_DOWN_THRESHOLD | 0.98 | 下跌2%对冲触发 |
| PRICE_UP_THRESHOLD | 1.02 | 上涨2%对冲/加仓触发 |
| PRICE_DOWN_HARD_THRESHOLD | 0.90 | 下跌10%硬阈值 |
| PRICE_UP_HARD_THRESHOLD | 1.10 | 上涨10%硬阈值 |
| LONG_ADD_THRESHOLD | 1.02 | 多头加仓价格阈值 |
| SHORT_ADD_THRESHOLD | 0.98 | 空头加仓价格阈值 |
| DATA_TIMEOUT | 180 | 数据超时时间(秒) |
| ORDER_INTERVAL | 0.1 | 下单间隔(秒) |

--------------------------------------------------------------------------------
17.3.3 下单类型
--------------------------------------------------------------------------------

| order_type | 说明 | 数量计算 |
|------------|------|---------|
| 0 | 初始开仓 | 按风控计算 |
| 1 | 对冲开仓 | = 对方持仓数量 |
| 2 | 翻倍加仓 | = 当前持仓数量 |
| 3, 5 | 翻倍平仓/日线关仓 | = 当前持仓数量 |
| 4 | 日线对冲 | = 对方持仓数量 |

--------------------------------------------------------------------------------
17.3.4 运行主循环
--------------------------------------------------------------------------------

```python
def _run_loop(self):
    while self.is_running:
        with distributed_lock(self.symbol + "main"):
            self.open_position()  # 核心开仓/平仓逻辑
            time.sleep(self.ORDER_INTERVAL)
```

关键点:
- 使用 Redis 分布式锁保证单实例执行
- `ORDER_INTERVAL = 0.1` 秒循环
- 锁超时 `REDIS_LOCK_TIMEOUT = 3` 秒

--------------------------------------------------------------------------------
17.3.5 open_position 核心逻辑
--------------------------------------------------------------------------------

**趋势行情处理** (MarketStatus.TREND + PinStatus.POS_LOCKED):

1. **保本平仓** (total_pnl >= 0):
   - 先平空头，再平多头
   - 设置 `Long_DAY_ALLOW` 或 `Short_DAY_ALLOW`

2. **指标平仓** (total_pnl < 0):
   - 多头平仓: `day_detector.check_long_entry()` → 平空头
   - 空头平仓: `day_detector.check_short_entry()` → 平多头
   - 日线对冲: `day_detector.check_long_hedge_condition()` → 做空对冲
   - 日线对冲: `day_detector.check_short_hedge_condition()` → 做多对冲

--------------------------------------------------------------------------------
17.3.6 PinStatus 状态机
--------------------------------------------------------------------------------

| 状态 | 说明 |
|------|------|
| INITIAL | 初始状态 |
| Long_INITIAL | 多头-初始 |
| Long_FIRST_OPEN | 多头-第一次开仓 |
| Long_DOUBLE_ADD | 多头-翻倍加仓 |
| Long_DAY_ALLOW | 日线多头-开放 |
| Short_INITIAL | 空头-初始 |
| Short_FIRST_OPEN | 空头-第一次开仓 |
| Short_DOUBLE_ADD | 空头-翻倍加仓 |
| Short_DAY_ALLOW | 日线空头-开放 |
| HEDGE_ENTER | 进入对冲 |
| POS_LOCKED | 仓位锁定 |

状态转换:
```
INITIAL ──> Long_INITIAL / Short_INITIAL
Long_INITIAL ──> Long_FIRST_OPEN ──> Long_DOUBLE_ADD ──> Long_DAY_ALLOW
Short_INITIAL ──> Short_FIRST_OPEN ──> Short_DOUBLE_ADD ──> Short_DAY_ALLOW
任意 ──> HEDGE_ENTER ──> POS_LOCKED
```

--------------------------------------------------------------------------------
17.3.7 风控引擎三层架构
--------------------------------------------------------------------------------

本文档描述基于旧代码 `risk_engine.py` 的风控引擎三层架构设计。

### 17.3.7.1 架构概览

```
┌─────────────────────────────────────────────────────────────────────────┐
│                        RiskEngine (顶级风控引擎)                          │
│  - 整合所有组件提供统一接口                                                │
│  - 自动从交易所API同步账户和仓位数据                                        │
│  - 统一接口: check_minute_order(), check_hour_order()                    │
└─────────────────────────────────────────────────────────────────────────┘
          │                           │                        │
          ▼                           ▼                        ▼
┌─────────────────┐      ┌─────────────────┐      ┌─────────────────────┐
│   AccountPool   │      │  StrategyPool   │      │     OrderCheck      │
│  (账户保证金池)  │      │  (策略保证金池)  │      │   (订单风控检查器)   │
│                 │      │                 │      │                     │
│ - 账户数据同步   │◄──── │ - 策略保证金分配 │◄──── │ - 订单级别风控检查   │
│ - Redis熔断保护 │      │ - 品种注册管理   │      │ - 保证金预占        │
│ - 本地缓存      │      │ - 开仓计算      │      │ - Lua脚本原子操作    │
└─────────────────┘      └─────────────────┘      └─────────────────────┘
```

### 17.3.7.2 AccountPool (账户保证金池)

职责:
1. 账户数据管理: 同步、缓存账户数据（总保证金、可用保证金等）
2. Redis熔断保护: 当Redis连续失败时启用本地缓存兜底
3. 数据一致性: 保证本地缓存与Redis数据的一致性

关键方法:
| 方法 | 说明 |
|------|------|
| update_account_data_from_redis() | 从Redis更新账户本地缓存 |
| get_account_margin() | 获取账户保证金信息 |

熔断机制:
- Redis连续失败3次触发熔断
- 熔断时有效保证金设为0，拒绝所有开仓
- 兜底时使用最低有效保证金 MIN_EFFECTIVE_MARGIN

AccountMargin 返回结构:
| 字段 | 说明 |
|------|------|
| total_margin_balance | 总保证金 |
| unrealized_pnl | 浮盈亏 |
| effective_margin | 有效保证金 = 总保证金 + 浮盈亏 |
| total_available_margin | 可用保证金 |
| reserve_margin | 保留保证金 |
| global_new_open_ceiling | 新开仓全局上限 |
| global_double_open_ceiling | 翻倍仓全局上限 |

### 17.3.7.3 StrategyPool (策略保证金池)

职责:
1. 策略保证金分配: 为不同时间级别的策略（分钟级、小时级）分配保证金
2. 开仓计算: 根据策略配置计算可开仓名义价值
3. 保证金占用: 管理策略实际占用的保证金和可用保证金
4. 品种注册: 与SymbolManager集成，管理分钟级品种注册

关键方法:
| 方法 | 说明 |
|------|------|
| register_symbol(symbol, strategy_level) | 注册品种到策略池 |
| calculate_strategy_margin(strategy_level) | 计算策略保证金分配 |
| calculate_minute_open_notional(leverage) | 计算分钟级可开仓名义价值 |
| calculate_hour_open_notional(leverage) | 计算小时级可开仓名义价值 |

分钟级开仓配置 (MINUTE_OPEN_CONFIG):
| 配置项 | 值 | 说明 |
|--------|-----|------|
| min_notional_per_symbol | 最低单品种名义价值 | 10 USDT |
| target_symbol_count | 目标品种数 | 5个 |
| threshold_amount | 阈值金额 | 100 USDT |

小时级开仓配置:
| 配置项 | 值 | 说明 |
|--------|-----|------|
| min_notional_per_symbol | 初始单品种名义价值 | 5 USDT |
| target_symbol_count | 目标品种数 | 10个 |
| add_times | 每品种加仓次数 | 10次 |
| threshold_notional | 总名义价值阈值 | 500 USDT |

### 17.3.7.4 OrderCheck (订单风控检查器)

职责:
1. 订单风控检查: 检查订单是否符合风控规则
2. 保证金预占: 使用Lua脚本原子性预占保证金，避免并发超开
3. 订单级别限制: 单品种最大开仓限制、策略级别限制等

关键方法:
| 方法 | 说明 |
|------|------|
| check_minute_order(symbol, notional, leverage, open_type) | 检查分钟级订单 |
| check_hour_order(symbol, notional, leverage) | 检查小时级订单 |
| _preoccupy_margin() | 原子预占保证金 |

预占校验Lua脚本:
```lua
local strategy_used_key = KEYS[1]
local required_margin = tonumber(ARGV[1])
local global_used = tonumber(ARGV[2])
local global_ceiling = tonumber(ARGV[3])
local strategy_allocation = tonumber(ARGV[4])

local current_used = tonumber(redis.call('get', strategy_used_key) or 0)
if current_used + required_margin > strategy_allocation then
    return 0 -- 策略额度不足
end
if global_used + required_margin > global_ceiling then
    return 0 -- 全局额度不足
end
redis.call('incrbyfloat', strategy_used_key, required_margin)
redis.call('expire', strategy_used_key, 30)
return 1 -- 预占成功
```

### 17.3.7.5 保证金配置

全局配置 (MARGIN_POOL_CONFIG["GLOBAL"]):
| 配置项 | 值 | 说明 |
|--------|-----|------|
| max_usage_ratio | 0.95 | 最大使用比例 |
| reserve_ratio | 0.05 | 保留比例 |

策略配置 (MARGIN_POOL_CONFIG["STRATEGY"]["MINUTE"]):
| 配置项 | 值 | 说明 |
|--------|-----|------|
| allocation_ratio | 0.60 | 分配比例(60%) |
| new_open_ratio | 0.10 | 新开仓比例 |
| double_open_ratio | 0.20 | 翻倍仓比例 |

策略配置 (MARGIN_POOL_CONFIG["STRATEGY"]["HOUR"]):
| 配置项 | 值 | 说明 |
|--------|-----|------|
| allocation_ratio | 0.40 | 分配比例(40%) |
| new_open_ratio | 0.15 | 新开仓比例 |

--------------------------------------------------------------------------------
17.3.8 盈亏管理模块
--------------------------------------------------------------------------------

本文档描述基于旧代码 `pnl_manager.py` 的盈亏管理模块设计。

### 17.3.8.1 核心理念

**低波动/高波动品种互斥机制**:
- 低波动品种: 需要被拯救的，盈亏计入总体盈亏
- 高波动品种: 拯救者，盈利用于覆盖低波动盈亏，但自己的盈亏不计入总盈亏
- 一次成功的高波动翻倍仓盈利可以覆盖多个低波动小仓位盈亏

### 17.3.8.2 核心数据结构

StrategyPositionPnl 结构:
| 字段 | 类型 | 说明 |
|------|------|------|
| symbol | String | 品种名称 |
| long_avg_price | f64 | 多头平均价 |
| long_qty | f64 | 多头数量 |
| short_avg_price | f64 | 空头平均价 |
| short_qty | f64 | 空头数量 |
| last_price | f64 | 最新价格 |
| unrealized_pnl | f64 | 浮盈亏 |
| update_ts | i64 | 更新时间戳 |

PnlCoverageResult 结构:
| 字段 | 类型 | 说明 |
|------|------|------|
| can_cover | bool | 能否覆盖 |
| total_unrealized_loss | f64 | 总浮亏金额 |
| current_symbol_profit | f64 | 当前品种盈利 |
| accumulated_profit | f64 | 累计盈利 |
| net_profit | f64 | 净盈利 |
| symbol_count | i32 | 低波动品种数 |

RescueResult 结构:
| 字段 | 类型 | 说明 |
|------|------|------|
| success | bool | 解救是否成功 |
| strategy_level | String | 策略级别 |
| can_rescue | bool | 能否解救 |
| total_loss | f64 | 总亏损 |
| rescued_symbols | Vec<String> | 已解救品种列表 |
| remaining_profit | f64 | 剩余盈利 |

### 17.3.8.3 核心方法

**update_position_pnl()**: 更新品种盈亏数据
- 输入: symbol, strategy_level, position_data
- 存储到Redis Hash: {level}_position_pnl

**mark_low_volatility_symbol()**: 标记低波动品种
- 互斥逻辑: 自动删除该品种的高波动标记
- 存储到Redis Set: {level}_low_volatility_symbols

**mark_high_volatility_symbol()**: 标记高波动品种
- 互斥逻辑: 自动删除该品种的低波动标记
- 存储到Redis Set: {level}_high_volatility_symbols

**check_pnl_coverage()**: 检查盈亏覆盖情况
- 仅针对低波动品种
- 高波动品种盈利用于覆盖低波动盈亏
- 浮盈仅做参考，不执行实际扣减

**rescue_low_volatility_symbols()**: 解救低波动品种
- 检查高波动盈利（当前+累计）能否覆盖所有低波动浮亏
- 如果能覆盖:
  - 删除低波动品种的盈亏记录
  - 清除低波动品种标记
  - 扣除覆盖所需的盈利，剩余部分累加到累计盈利
  - 记录已解救品种到历史
- 如果不能覆盖: 返回失败

**add_accumulated_profit()**: 添加累计盈利
- 用于已实现盈利的累加

**reset_strategy()**: 重置策略盈亏数据
- 清除所有盈亏数据
- 清除低/高波动品种标记
- 清除解救历史记录

### 17.3.8.4 Redis Key 设计

| Key Pattern | 类型 | 说明 |
|-------------|------|------|
| {level}_position_pnl | Hash | 品种盈亏数据 |
| {level}_low_volatility_symbols | Set | 低波动品种列表 |
| {level}_high_volatility_symbols | Set | 高波动品种列表 |
| {level}_accumulated_profit | String | 累计盈利 |
| {level}_rescued_symbols | List | 已解救品种历史 |

### 17.3.8.5 解救流程示例

```
1. 高波动品种 BTCUSDT 盈利 200 USDT (已实现)
2. 低波动品种 ETHUSDT 浮亏 -80 USDT
3. 低波动品种 SOLUSDT 浮亏 -50 USDT
4. 总低波动浮亏 = -130 USDT

检查盈亏覆盖:
- total_loss = 130 USDT
- total_available_profit = 200 + accumulated_profit
- can_cover = total_available_profit >= total_loss

如果可以覆盖:
- 删除 ETHUSDT, SOLUSDT 盈亏记录
- 清除低波动标记
- remaining_profit = 200 - 130 = 70 USDT (累加到累计盈利)
- 记录解救历史
```

--------------------------------------------------------------------------------
17.3.9 交易对规则模块 (SymbolRules)
--------------------------------------------------------------------------------

本文档描述基于旧代码 `symbol_rule_service.py` 的交易对规则模块设计。

### 17.3.9.1 核心组件

| 组件 | 类型 | 说明 |
|------|------|------|
| SymbolRules | 数据模型 | 交易对规则不可变对象 |
| SymbolRuleParser | 解析器 | 从Redis读取并解析交易对规则 |
| LeverageCommissionCache | 缓存管理器 | 杠杆档位/手续费数据缓存 |

### 17.3.9.2 SymbolRules 数据模型

```
@dataclass(frozen=True)
struct SymbolRules {
    symbol:             String,  // 交易对名称
    price_precision:    u8,      // 价格精度
    quantity_precision: u8,      // 数量精度
    tick_size:          f64,     // 最小价格变动
    min_qty:            f64,     // 交易所原始最小数量
    min_notional:       f64,     // 最小名义价值
    max_notional:       f64,     // 最大名义价值
    leverage:           u32,     // 杠杆倍数
    maker_fee:          f64,     // 挂单手续费率
    taker_fee:          f64,     // 吃单手续费率
    close_min_ratio:    f64,     // 平仓最小盈亏比阈值
    min_value_threshold:f64,     // 下单最小名义价值阈值
    update_ts:          i64,     // 规则更新时间戳
}
```

关键属性:
| 属性 | 说明 |
|------|------|
| effective_min_qty | 实际有效最小开仓数量（自动计算） |
| step_size | 数量最小步进 = 10^(-quantity_precision) |

### 17.3.9.3 effective_min_qty 计算逻辑

原始最小数量(min_qty)不能直接使用，需计算实际有效最小开仓数量:

```
1. 理论最小数量 = max(min_notional / tick_size, min_qty)
2. 按数量精度取整
3. 校验取整后仍满足最小名义价值
```

### 17.3.9.4 核心方法

**round_price()**: 价格取整
- 按 tick_size 步进取整
- 按 price_precision 精度取舍

**round_qty()**: 数量取整
- 使用 effective_min_qty 确保不低于最低数量
- 按 quantity_precision 精度取舍

**validate_order()**: 订单校验
- 检查数量 >= effective_min_qty
- 检查名义价值 >= min_value_threshold

**calculate_open_qty()**: 基于名义价值计算合规开仓数量
- 使用 Decimal 保证高精度计算
- 确保数量 >= effective_min_qty
- 按步进取整，最终验证名义价值达标

### 17.3.9.5 SymbolRuleParser 解析器

职责:
1. 从Redis读取主交易对规则 (symbol_rules Hash)
2. 获取杠杆档位和手续费率 (优先缓存，1小时刷新)
3. 解析转换为标准 SymbolRules 对象

缓存策略:
| 数据类型 | 缓存Key | 刷新间隔 |
|----------|---------|----------|
| 主规则 | symbol_rules | 实时 |
| 杠杆档位 | leverage_brackets | 1小时 |
| 手续费率 | commission_info | 1小时 |

### 17.3.9.6 Redis Key 设计

| Key | 类型 | 说明 |
|-----|------|------|
| symbol_rules | Hash | 主交易对规则 |
| leverage_brackets | Hash | 杠杆档位缓存 |
| commission_info | Hash | 手续费率缓存 |

### 17.3.9.7 使用示例

```rust
// 获取交易对规则
let parser = SymbolRuleParser::new();
let rules = parser.get_symbol_rules("BTCUSDT").unwrap();

// 价格取整
let price = rules.round_price(68000.123456);  // → 68000.12

// 数量取整
let qty = rules.round_qty(0.0001);  // → 有效最小数量

// 订单校验
let is_valid = rules.validate_order(price, qty);

// 基于名义价值计算开仓数量
let qty = rules.calculate_open_qty(10.0, 68000.0);  // 10 USDT @ 68000
```

================================================================================
