# v1.2 市场数据获取和策略触发 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 构建高性能市场数据获取层，支持 1500+ 交易品种的 K线订阅、订单簿深度订阅、滑动窗口波动率检测、历史K线 Redis 持久化。

**Architecture:** 4 个独立 WebSocket 连接 (2×1m K线 + 1d K线 + Depth)，分片订阅 + 速控限制，VolatilityDetector 内部合成 15m K线，Redis 存储历史K线和品种规则。

**Tech Stack:** Rust, tokio, tokio-tungstenite, rust_decimal, rust_decimal_macros, redis, fnv, reqwest

---

## 文件结构

```
crates/market/src/
├── lib.rs                    # 模块导出
├── types.rs                  # 类型定义 (扩展)
├── binance_ws.rs             # WebSocket 连接 (扩展: subscribe/unsubscribe)
├── kline.rs                  # K线合成器 (扩展)
├── orderbook.rs              # 新增: 订单簿 (20档 + 深度指标)
├── volatility.rs             # 新增: 波动率检测器
├── symbol_registry.rs         # 新增: 品种注册中心
├── data_feeder.rs            # 新增: 核心 DataFeeder
└── kline_persistence.rs      # 新增: K线 Redis 持久化

crates/market/Cargo.toml      # 添加 redis, fnv, reqwest, rust_decimal_macros 依赖
```

---

## Task 1: 依赖配置

**Files:**
- Modify: `crates/market/Cargo.toml`

- [ ] **Step 1: 添加依赖**

```toml
[dependencies]
redis = { version = "0.27", features = ["tokio-comp", "connection-manager"] }
fnv = "1.49"
reqwest = { version = "0.12", features = ["json"] }
rust_decimal_macros = "1.38"
```

- [ ] **Step 2: Commit**

```bash
git add crates/market/Cargo.toml
git commit -m "chore(market): 添加 redis, fnv, reqwest, rust_decimal_macros 依赖"
```

---

## Task 2: 扩展 BinanceWsConnector (先实现订阅方法)

**Files:**
- Modify: `crates/market/src/binance_ws.rs`

- [ ] **Step 1: 添加订阅/退订方法到 binance_ws.rs**

在现有 BinanceWsConnector 实现中添加:

```rust
impl BinanceWsConnector {
    /// 发送订阅消息
    pub async fn subscribe(&mut self, streams: &[String]) -> Result<(), MarketError> {
        let msg = serde_json::json!({
            "method": "SUBSCRIBE",
            "params": streams,
            "id": chrono::Utc::now().timestamp_millis()
        });

        let text = serde_json::to_string(&msg)
            .map_err(|e| MarketError::SerializeError(e.to_string()))?;

        self.ws_stream.write_message(Message::Text(text.into()))
            .await
            .map_err(|e| MarketError::WebSocketError(e.to_string()))?;

        Ok(())
    }

    /// 发送退订消息
    pub async fn unsubscribe(&mut self, streams: &[String]) -> Result<(), MarketError> {
        let msg = serde_json::json!({
            "method": "UNSUBSCRIBE",
            "params": streams,
            "id": chrono::Utc::now().timestamp_millis()
        });

        let text = serde_json::to_string(&msg)
            .map_err(|e| MarketError::SerializeError(e.to_string()))?;

        self.ws_stream.write_message(Message::Text(text.into()))
            .await
            .map_err(|e| MarketError::WebSocketError(e.to_string()))?;

        Ok(())
    }
}
```

- [ ] **Step 2: Commit**

```bash
git add crates/market/src/binance_ws.rs
git commit -m "feat(market): BinanceWsConnector 添加 subscribe/unsubscribe 方法"
```

---

## Task 3: OrderBook 订单簿模块

**Files:**
- Create: `crates/market/src/orderbook.rs`
- Modify: `crates/market/src/lib.rs`

- [ ] **Step 1: 创建 orderbook.rs**

```rust
//! 订单簿模块 - 20档深度 + 深度指标计算

use rust_decimal::Decimal;
use rust_decimal_macros::dec;

pub struct OrderBook {
    symbol: String,
    bids: Vec<(Decimal, Decimal)>,  // (price, qty)
    asks: Vec<(Decimal, Decimal)>,
    last_update_id: u64,
}

impl OrderBook {
    pub fn new(symbol: String) -> Self {
        Self {
            symbol,
            bids: Vec::with_capacity(20),
            asks: Vec::with_capacity(20),
            last_update_id: 0,
        }
    }

    /// 深度指标: 买盘/卖盘 厚度比率
    pub fn depth_indicator(&self) -> Decimal {
        let bid_depth: Decimal = self.bids.iter().take(20).map(|(_, q)| q).sum();
        let ask_depth: Decimal = self.asks.iter().take(20).map(|(_, q)| q).sum();
        if ask_depth > dec!(0) {
            bid_depth / ask_depth
        } else {
            dec!(1)
        }
    }

    /// 更新订单簿 (增量更新)
    pub fn update(&mut self, last_update_id: u64, bids: Vec<(Decimal, Decimal)>, asks: Vec<(Decimal, Decimal)>) {
        // 确保 update_id 递增
        if last_update_id > self.last_update_id {
            self.bids = bids;
            self.asks = asks;
            self.last_update_id = last_update_id;
        }
    }

    pub fn last_update_id(&self) -> u64 {
        self.last_update_id
    }

    pub fn symbol(&self) -> &str {
        &self.symbol
    }

    pub fn best_bid(&self) -> Option<Decimal> {
        self.bids.first().map(|(p, _)| *p)
    }

    pub fn best_ask(&self) -> Option<Decimal> {
        self.asks.first().map(|(p, _)| *p)
    }

    /// 订单簿总档数
    pub fn depth(&self) -> (usize, usize) {
        (self.bids.len(), self.asks.len())
    }
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn test_depth_indicator() {
        let mut ob = OrderBook::new("BTCUSDT".to_string());
        ob.update(1,
            vec![(dec!(100), dec!(10)), (dec!(99), dec!(20))],
            vec![(dec!(101), dec!(15)), (dec!(102), dec!(25))],
        );

        let indicator = ob.depth_indicator();
        // bid_depth = 10 + 20 = 30
        // ask_depth = 15 + 25 = 40
        // ratio = 30 / 40 = 0.75
        assert!(indicator < dec!(0.8) && indicator > dec!(0.7));
    }
}
```

- [ ] **Step 2: 在 lib.rs 添加模块导出**

```rust
pub mod orderbook;
pub use orderbook::OrderBook;
```

- [ ] **Step 3: Commit**

```bash
git add crates/market/src/orderbook.rs crates/market/src/lib.rs
git commit -m "feat(market): 添加 OrderBook 模块和 depth_indicator 计算"
```

---

## Task 4: VolatilityDetector 波动率检测器

**Files:**
- Create: `crates/market/src/volatility.rs`
- Modify: `crates/market/src/lib.rs`

- [ ] **Step 1: 创建 volatility.rs**

```rust
//! 波动率检测器 - 1m O-C 和 15m Close-Close 变化率

use rust_decimal::Decimal;
use rust_decimal_macros::dec;
use crate::kline::KLineSynthesizer;
use crate::types::{KLine, Period, VolatilityStats, Tick};

pub struct VolatilityDetector {
    /// 1m K线合成器
    kline_1m: KLineSynthesizer,
    /// 15m K线合成器 (内部合成)
    kline_15m: KLineSynthesizer,
    /// 15m 滑动窗口 (保留最近2根)
    kline_15m_window: Vec<KLine>,
    /// 1m K线计数器 (用于判断是否满15根)
    kline_1m_count: u32,
    /// 阈值: 1m 3%
    threshold_1m: Decimal,
    /// 阈值: 15m 13%
    threshold_15m: Decimal,
}

impl VolatilityDetector {
    pub fn new(symbol: String) -> Self {
        Self {
            kline_1m: KLineSynthesizer::new(symbol.clone(), Period::Minute(1)),
            kline_15m: KLineSynthesizer::new(symbol.clone(), Period::Minute(15)),
            kline_15m_window: Vec::with_capacity(2),
            kline_1m_count: 0,
            threshold_1m: dec!(0.03),
            threshold_15m: dec!(0.13),
        }
    }

    /// 更新并检测波动率
    /// 返回 VolatilityStats
    pub fn update(&mut self, price: Decimal, timestamp: chrono::DateTime<chrono::Utc>) -> VolatilityStats {
        // 1. 创建 Tick
        let tick = Tick {
            symbol: self.kline_1m.symbol.clone(),
            price,
            qty: dec!(1),
            timestamp,
        };

        // 2. 更新 1m K线
        let completed_1m = self.kline_1m.update(&tick);
        let kline_1m = match self.kline_1m.current_kline() {
            Some(k) => k.clone(),
            None => return VolatilityStats::default(),
        };

        // 3. 计算 1m O-C 变化率
        let vol_1m = self.calc_1m_volatility(&kline_1m);

        // 4. 更新 15m K线 (每15根1m K线合成1根15m)
        if completed_1m.is_some() {
            self.kline_1m_count += 1;
            if self.kline_1m_count >= 15 {
                // 用完整的1m K线更新15m合成器
                let _ = self.kline_15m.update(&tick);
                self.kline_1m_count = 0;
            }
        }

        // 5. 检查15m是否完成，更新窗口
        if let Some(completed_15m) = self.kline_15m.update(&tick) {
            self.update_15m_window(completed_15m);
        }

        // 6. 计算 15m Close-Close 变化率
        let vol_15m = self.calc_15m_volatility();

        // 7. 判断是否高波动
        let is_high = vol_1m >= self.threshold_1m || vol_15m >= self.threshold_15m;

        VolatilityStats {
            is_high_volatility: is_high,
            vol_1m,
            vol_15m,
        }
    }

    /// 计算 1m O-C 变化率
    fn calc_1m_volatility(&self, kline: &KLine) -> Decimal {
        if kline.open > dec!(0) {
            (kline.close - kline.open).abs() / kline.open
        } else {
            dec!(0)
        }
    }

    /// 计算 15m Close-Close 变化率
    fn calc_15m_volatility(&self) -> Decimal {
        if self.kline_15m_window.len() < 2 {
            return dec!(0);
        }

        let prev = &self.kline_15m_window[0];
        let curr = &self.kline_15m_window[1];

        if prev.close > dec!(0) {
            (curr.close - prev.close).abs() / prev.close
        } else {
            dec!(0)
        }
    }

    /// 更新 15m 窗口
    fn update_15m_window(&mut self, kline: KLine) {
        if self.kline_15m_window.len() >= 2 {
            self.kline_15m_window.remove(0);
        }
        self.kline_15m_window.push(kline);
    }

    pub fn thresholds(&self) -> (Decimal, Decimal) {
        (self.threshold_1m, self.threshold_15m)
    }
}

impl Default for VolatilityStats {
    fn default() -> Self {
        Self {
            is_high_volatility: false,
            vol_1m: dec!(0),
            vol_15m: dec!(0),
        }
    }
}
```

- [ ] **Step 2: 添加 VolatilityStats Default 实现到 types.rs**

```rust
impl Default for VolatilityStats {
    fn default() -> Self {
        Self {
            is_high_volatility: false,
            vol_1m: dec!(0),
            vol_15m: dec!(0),
        }
    }
}
```

- [ ] **Step 3: 在 lib.rs 添加模块导出**

```rust
pub mod volatility;
pub use volatility::VolatilityDetector;
```

- [ ] **Step 4: Commit**

```bash
git add crates/market/src/volatility.rs crates/market/src/types.rs crates/market/src/lib.rs
git commit -m "feat(market): 添加 VolatilityDetector 波动率检测器"
```

---

## Task 5: SymbolRegistry 品种注册中心

**Files:**
- Create: `crates/market/src/symbol_registry.rs`
- Modify: `crates/market/src/lib.rs`

- [ ] **Step 1: 创建 symbol_registry.rs**

```rust
//! 品种注册中心 - Redis 存储 + 订阅管理

use fnv::FnvHashSet;
use redis::AsyncCommands;
use std::sync::Arc;
use tokio::sync::RwLock;

pub struct SymbolRegistry {
    redis: redis::aio::ConnectionManager,
    trading_symbols: RwLock<FnvHashSet<String>>,
    last_update: std::time::Instant,
    update_interval: std::time::Duration,
}

impl SymbolRegistry {
    pub async fn new(redis_url: &str) -> Result<Self, crate::error::MarketError> {
        let client = redis::Client::open(redis_url)
            .map_err(|e| crate::error::MarketError::RedisError(e.to_string()))?;
        let conn = client.get_connection_manager()
            .map_err(|e| crate::error::MarketError::RedisError(e.to_string()))?;

        Ok(Self {
            redis: conn,
            trading_symbols: RwLock::new(FnvHashSet::default()),
            last_update: std::time::Instant::now(),
            update_interval: std::time::Duration::from_secs(120),  // 2分钟
        })
    }

    /// 从 Binance 获取交易对信息并更新 Redis
    pub async fn update_symbols(&mut self) -> Result<(), crate::error::MarketError> {
        // 1. 获取 exchangeInfo
        let client = reqwest::Client::new();
        let resp = client
            .get("https://fapi.binance.com/fapi/v1/exchangeInfo")
            .send()
            .await
            .map_err(|e| crate::error::MarketError::NetworkError(e.to_string()))?;

        let info: serde_json::Value = resp
            .json()
            .await
            .map_err(|e| crate::error::MarketError::ParseError(e.to_string()))?;

        // 2. 解析并存储
        if let Some(symbols) = info.get("symbols").and_then(|s| s.as_array()) {
            let mut new_symbols = FnvHashSet::default();

            for symbol_info in symbols {
                if let (Some(symbol), Some(status)) =
                    (symbol_info.get("symbol").and_then(|s| s.as_str()),
                     symbol_info.get("status").and_then(|s| s.as_str()))
                {
                    if status == "TRADING" {
                        // 存储到 Redis Hash
                        let key = format!("exchangeInfo:{}", symbol);
                        let json = symbol_info.to_string();
                        let _: () = self.redis.hset("exchangeInfo", symbol, &json).await
                            .map_err(|e| crate::error::MarketError::RedisError(e.to_string()))?;

                        new_symbols.insert(symbol.to_string());
                    }
                }
            }

            // 更新内存中的交易品种
            let mut symbols_guard = self.trading_symbols.write().await;
            *symbols_guard = new_symbols;
        }

        self.last_update = std::time::Instant::now();
        Ok(())
    }

    /// 检查是否需要更新
    pub fn needs_update(&self) -> bool {
        self.last_update.elapsed() >= self.update_interval
    }

    /// 获取所有交易品种
    pub async fn get_trading_symbols(&self) -> FnvHashSet<String> {
        self.trading_symbols.read().await.clone()
    }

    /// 获取品种规则
    pub async fn get_symbol_info(&self, symbol: &str) -> Option<String> {
        let info: Option<String> = self.redis.hget("exchangeInfo", symbol).await.ok()?;
        info
    }
}
```

- [ ] **Step 2: 在 lib.rs 添加模块导出**

```rust
pub mod symbol_registry;
pub use symbol_registry::SymbolRegistry;
```

- [ ] **Step 3: Commit**

```bash
git add crates/market/src/symbol_registry.rs crates/market/src/lib.rs
git commit -m "feat(market): 添加 SymbolRegistry 品种注册中心"
```

---

## Task 6: KlinePersistence K线 Redis 持久化

**Files:**
- Create: `crates/market/src/kline_persistence.rs`
- Modify: `crates/market/src/lib.rs`

- [ ] **Step 1: 创建 kline_persistence.rs**

```rust
//! K线 Redis 持久化 - 1m/15m/1d 历史K线存储

const KLINE_1M_MAX: isize = 320;
const KLINE_15M_MAX: isize = 100;
const KLINE_1D_MAX: isize = 100;

pub struct KlinePersistence {
    redis: redis::aio::ConnectionManager,
}

impl KlinePersistence {
    pub async fn new(redis_url: &str) -> Result<Self, crate::error::MarketError> {
        let client = redis::Client::open(redis_url)
            .map_err(|e| crate::error::MarketError::RedisError(e.to_string()))?;
        let conn = client.get_connection_manager()
            .map_err(|e| crate::error::MarketError::RedisError(e.to_string()))?;

        Ok(Self { redis: conn })
    }

    /// 存储 1m K线
    pub async fn push_1m(&mut self, symbol: &str, kline_json: &str) -> Result<(), crate::error::MarketError> {
        let key = format!("kline:1m:{}", symbol);
        let _: () = self.redis.lpush(&key, kline_json).await
            .map_err(|e| crate::error::MarketError::RedisError(e.to_string()))?;
        let _: () = self.redis.ltrim(&key, 0, KLINE_1M_MAX - 1).await
            .map_err(|e| crate::error::MarketError::RedisError(e.to_string()))?;
        Ok(())
    }

    /// 存储 15m K线
    pub async fn push_15m(&mut self, symbol: &str, kline_json: &str) -> Result<(), crate::error::MarketError> {
        let key = format!("kline:15m:{}", symbol);
        let _: () = self.redis.lpush(&key, kline_json).await
            .map_err(|e| crate::error::MarketError::RedisError(e.to_string()))?;
        let _: () = self.redis.ltrim(&key, 0, KLINE_15M_MAX - 1).await
            .map_err(|e| crate::error::MarketError::RedisError(e.to_string()))?;
        Ok(())
    }

    /// 存储 1d K线
    pub async fn push_1d(&mut self, symbol: &str, kline_json: &str) -> Result<(), crate::error::MarketError> {
        let key = format!("kline:1d:{}", symbol);
        let _: () = self.redis.lpush(&key, kline_json).await
            .map_err(|e| crate::error::MarketError::RedisError(e.to_string()))?;
        let _: () = self.redis.ltrim(&key, 0, KLINE_1D_MAX - 1).await
            .map_err(|e| crate::error::MarketError::RedisError(e.to_string()))?;
        Ok(())
    }

    /// 读取 1m K线
    pub async fn get_1m(&mut self, symbol: &str) -> Result<Vec<String>, crate::error::MarketError> {
        let key = format!("kline:1m:{}", symbol);
        let klines: Vec<String> = self.redis.lrange(&key, 0, -1).await
            .map_err(|e| crate::error::MarketError::RedisError(e.to_string()))?;
        Ok(klines)
    }

    /// 读取 15m K线
    pub async fn get_15m(&mut self, symbol: &str) -> Result<Vec<String>, crate::error::MarketError> {
        let key = format!("kline:15m:{}", symbol);
        let klines: Vec<String> = self.redis.lrange(&key, 0, -1).await
            .map_err(|e| crate::error::MarketError::RedisError(e.to_string()))?;
        Ok(klines)
    }

    /// 读取 1d K线
    pub async fn get_1d(&mut self, symbol: &str) -> Result<Vec<String>, crate::error::MarketError> {
        let key = format!("kline:1d:{}", symbol);
        let klines: Vec<String> = self.redis.lrange(&key, 0, -1).await
            .map_err(|e| crate::error::MarketError::RedisError(e.to_string()))?;
        Ok(klines)
    }
}
```

- [ ] **Step 2: 在 lib.rs 添加模块导出**

```rust
pub mod kline_persistence;
pub use kline_persistence::KlinePersistence;
```

- [ ] **Step 3: Commit**

```bash
git add crates/market/src/kline_persistence.rs crates/market/src/lib.rs
git commit -m "feat(market): 添加 KlinePersistence K线 Redis 持久化"
```

---

## Task 7: DataFeeder 核心数据分发器

**Files:**
- Create: `crates/market/src/data_feeder.rs`
- Modify: `crates/market/src/lib.rs`

- [ ] **Step 1: 创建 data_feeder.rs**

```rust
//! DataFeeder - 核心数据分发器，统一管理 4 个 WebSocket 连接

use crate::binance_ws::BinanceWsConnector;
use crate::orderbook::OrderBook;
use crate::volatility::VolatilityDetector;
use crate::symbol_registry::SymbolRegistry;
use crate::types::KLine;
use fnv::FnvHashMap;
use fnv::FnvHashSet;
use futures_util::StreamExt;
use std::sync::Arc;
use tokio::sync::{broadcast, RwLock};
use tokio::time::{sleep, Duration};

const PAGINATE_SIZE: usize = 50;
const PAGINATE_INTERVAL_MS: u64 = 500;

pub struct DataFeeder {
    // 4 个 WebSocket 连接
    kline_1m_conn_1: RwLock<Option<BinanceWsConnector>>,
    kline_1m_conn_2: RwLock<Option<BinanceWsConnector>>,
    kline_1d_conn: RwLock<Option<BinanceWsConnector>>,
    depth_conn: RwLock<Option<BinanceWsConnector>>,

    // 广播通道
    broadcaster: broadcast::Sender<DataMessage>,

    // 波动率检测器 (按品种)
    volatility_detectors: RwLock<FnvHashMap<String, VolatilityDetector>>,

    // 订单簿 (按品种)
    orderbooks: RwLock<FnvHashMap<String, OrderBook>>,

    // Depth 订阅管理器
    depth_subscribed: RwLock<FnvHashSet<String>>,
    default_depth_symbol: String,

    // 品种注册
    symbol_registry: Arc<RwLock<SymbolRegistry>>,

    // 状态
    is_initialized: RwLock<bool>,
    last_data_time: RwLock<std::time::Instant>,
}

pub enum DataMessage {
    KLine1m { symbol: String, kline: KLine },
    KLine1d { symbol: String, kline: KLine },
    Depth { symbol: String, orderbook: OrderBook },
}

impl DataFeeder {
    pub async fn new(redis_url: &str) -> Result<Self, crate::error::MarketError> {
        let (tx, _) = broadcast::channel(1024);
        let symbol_registry = SymbolRegistry::new(redis_url).await?;

        Ok(Self {
            kline_1m_conn_1: RwLock::new(None),
            kline_1m_conn_2: RwLock::new(None),
            kline_1d_conn: RwLock::new(None),
            depth_conn: RwLock::new(None),
            broadcaster: tx,
            volatility_detectors: RwLock::new(FnvHashMap::default()),
            orderbooks: RwLock::new(FnvHashMap::default()),
            depth_subscribed: RwLock::new(FnvHashSet::default()),
            default_depth_symbol: "BTCUSDT".to_string(),
            symbol_registry: Arc::new(RwLock::new(symbol_registry)),
            is_initialized: RwLock::new(false),
            last_data_time: RwLock::new(std::time::Instant::now()),
        })
    }

    /// 启动 DataFeeder
    pub async fn start(&self) -> Result<(), crate::error::MarketError> {
        // 1. 更新品种列表
        {
            let mut registry = self.symbol_registry.write().await;
            registry.update_symbols().await?;
        }

        // 2. 获取品种列表
        let symbols = {
            let registry = self.symbol_registry.read().await;
            registry.get_trading_symbols().await
        };
        let symbols_vec: Vec<String> = symbols.into_iter().collect();

        // 3. 建立连接并分片订阅
        self.init_kline_1m_connections(&symbols_vec).await?;
        self.init_kline_1d_connection(&symbols_vec).await?;
        self.init_depth_connection().await?;

        // 4. 标记初始化完成
        {
            let mut initialized = self.is_initialized.write().await;
            *initialized = true;
        }

        // 5. 启动后台任务
        let feader_clone = self.clone();
        tokio::spawn(async move {
            feader_clone.data_monitor_loop().await;
        });

        let feader_clone = self.clone();
        tokio::spawn(async move {
            feader_clone.symbol_update_loop().await;
        });

        Ok(())
    }

    /// 初始化 1m K线连接 (两组)
    async fn init_kline_1m_connections(&self, symbols: &[String]) -> Result<(), crate::error::MarketError> {
        let mid = symbols.len() / 2;

        // 连接1: symbols[0..mid]
        let conn1 = BinanceWsConnector::new("1m_group1");
        self.subscribe_kline_batch_with_conn(&conn1, &symbols[..mid], "1m").await?;
        *self.kline_1m_conn_1.write().await = Some(conn1);

        // 连接2: symbols[mid..]
        let conn2 = BinanceWsConnector::new("1m_group2");
        self.subscribe_kline_batch_with_conn(&conn2, &symbols[mid..], "1m").await?;
        *self.kline_1m_conn_2.write().await = Some(conn2);

        Ok(())
    }

    /// 初始化 1d K线连接
    async fn init_kline_1d_connection(&self, symbols: &[String]) -> Result<(), crate::error::MarketError> {
        let conn = BinanceWsConnector::new("1d");
        self.subscribe_kline_batch_with_conn(&conn, symbols, "1d").await?;
        *self.kline_1d_conn.write().await = Some(conn);
        Ok(())
    }

    /// 初始化 Depth 连接
    async fn init_depth_connection(&self) -> Result<(), crate::error::MarketError> {
        let mut conn = BinanceWsConnector::new("depth");
        let stream = format!("{}@depth@100ms", self.default_depth_symbol.to_lowercase());
        conn.subscribe(&[stream]).await?;
        *self.depth_conn.write().await = Some(conn);

        let mut subscribed = self.depth_subscribed.write().await;
        subscribed.insert(self.default_depth_symbol.clone());

        Ok(())
    }

    /// 分片订阅 K线
    async fn subscribe_kline_batch_with_conn(
        &self,
        conn: &BinanceWsConnector,
        symbols: &[String],
        interval: &str,
    ) -> Result<(), crate::error::MarketError> {
        let mut conn = conn.clone();
        for chunk in symbols.chunks(PAGINATE_SIZE) {
            let streams: Vec<String> = chunk.iter()
                .map(|s| format!("{}@kline_{}", s.to_lowercase(), interval))
                .collect();

            conn.subscribe(&streams).await?;
            sleep(Duration::from_millis(PAGINATE_INTERVAL_MS)).await;
        }
        Ok(())
    }

    /// 数据监控循环
    async fn data_monitor_loop(&self) {
        loop {
            // 检查是否初始化
            let is_init = *self.is_initialized.read().await;
            if !is_init {
                sleep(Duration::from_secs(1)).await;
                continue;
            }

            // 检查60秒无数据告警
            let last_time = *self.last_data_time.read().await;
            if last_time.elapsed() > Duration::from_secs(60) {
                tracing::error!("数据监控: 60秒无数据!");
                // 触发告警/重启逻辑
            }

            sleep(Duration::from_secs(10)).await;
        }
    }

    /// 品种更新循环
    async fn symbol_update_loop(&self) {
        loop {
            sleep(Duration::from_secs(120)).await;

            let needs_update = {
                let registry = self.symbol_registry.read().await;
                registry.needs_update()
            };

            if needs_update {
                let mut registry = self.symbol_registry.write().await;
                if let Err(e) = registry.update_symbols().await {
                    tracing::error!("品种更新失败: {}", e);
                }
            }
        }
    }

    /// 订阅
    pub fn subscribe(&self) -> broadcast::Receiver<DataMessage> {
        self.broadcaster.subscribe()
    }

    /// 添加高波动品种 Depth 订阅
    pub async fn add_depth_subscription(&self, symbol: &str) -> Result<(), crate::error::MarketError> {
        {
            let subscribed = self.depth_subscribed.read().await;
            if subscribed.contains(symbol) {
                return Ok(());
            }
        }

        let mut conn = self.depth_conn.write().await;
        if let Some(ref mut c) = *conn {
            let stream = format!("{}@depth@100ms", symbol.to_lowercase());
            c.subscribe(&[stream]).await?;
        }

        let mut subscribed = self.depth_subscribed.write().await;
        subscribed.insert(symbol.to_string());

        Ok(())
    }

    /// 移除高波动品种 Depth 订阅
    pub async fn remove_depth_subscription(&self, symbol: &str) -> Result<(), crate::error::MarketError> {
        if symbol == self.default_depth_symbol {
            return Ok(());
        }

        {
            let subscribed = self.depth_subscribed.read().await;
            if !subscribed.contains(symbol) {
                return Ok(());
            }
        }

        let mut conn = self.depth_conn.write().await;
        if let Some(ref mut c) = *conn {
            let stream = format!("{}@depth@100ms", symbol.to_lowercase());
            c.unsubscribe(&[stream]).await?;
        }

        let mut subscribed = self.depth_subscribed.write().await;
        subscribed.remove(symbol);

        Ok(())
    }
}

impl Clone for DataFeeder {
    fn clone(&self) -> Self {
        Self {
            kline_1m_conn_1: RwLock::new(None),
            kline_1m_conn_2: RwLock::new(None),
            kline_1d_conn: RwLock::new(None),
            depth_conn: RwLock::new(None),
            broadcaster: self.broadcaster.clone(),
            volatility_detectors: RwLock::new(FnvHashMap::default()),
            orderbooks: RwLock::new(FnvHashMap::default()),
            depth_subscribed: RwLock::new(FnvHashSet::default()),
            default_depth_symbol: self.default_depth_symbol.clone(),
            symbol_registry: self.symbol_registry.clone(),
            is_initialized: RwLock::new(false),
            last_data_time: RwLock::new(std::time::Instant::now()),
        }
    }
}
```

- [ ] **Step 2: 更新 lib.rs 添加 DataFeeder 导出**

```rust
pub mod data_feeder;
pub use data_feeder::{DataFeeder, DataMessage};
```

- [ ] **Step 3: Commit**

```bash
git add crates/market/src/data_feeder.rs crates/market/src/lib.rs
git commit -m "feat(market): 添加 DataFeeder 核心数据分发器"
```

---

## Task 8: 编译验证

- [ ] **Step 1: 运行 cargo check**

```bash
cd crates/market && cargo check 2>&1
```

- [ ] **Step 2: 修复编译错误**

根据错误信息修复类型不匹配、缺失实现等问题。

- [ ] **Step 3: Commit**

```bash
git add -A && git commit -m "fix(market): 编译错误修复"
```

---

## Task 9: 单元测试

- [ ] **Step 1: 测试 OrderBook depth_indicator**

```bash
cd crates/market && cargo test orderbook -- --nocapture
```

- [ ] **Step 2: 测试 VolatilityDetector**

```bash
cd crates/market && cargo test volatility -- --nocapture
```

- [ ] **Step 3: Commit**

```bash
git add -A && cargo test
git commit -m "test(market): OrderBook 和 VolatilityDetector 单元测试"
```

---

## Task 10: 高可用机制 (补充)

**Files:**
- Modify: `crates/market/src/binance_ws.rs`
- Modify: `crates/market/src/data_feeder.rs`

- [ ] **Step 1: 添加重连逻辑到 BinanceWsConnector**

```rust
impl BinanceWsConnector {
    /// 重连 (指数退避)
    pub async fn reconnect_with_backoff(&mut self) -> Result<(), MarketError> {
        let mut backoff = Duration::from_secs(5);
        let max_backoff = Duration::from_secs(120);

        loop {
            tracing::info!("WebSocket 重连中, 等待 {:?}...", backoff);
            sleep(backoff).await;

            match self.connect().await {
                Ok(_) => {
                    tracing::info!("WebSocket 重连成功");
                    return Ok(());
                }
                Err(e) => {
                    tracing::warn!("WebSocket 重连失败: {}", e);
                    backoff = (backoff * 2).min(max_backoff);
                }
            }
        }
    }
}
```

- [ ] **Step 2: Commit**

```bash
git add crates/market/src/binance_ws.rs crates/market/src/data_feeder.rs
git commit -m "feat(market): 添加 WebSocket 重连指数退避机制"
```

---

## 验收标准

- [ ] 支持 1500+ 品种订阅
- [ ] 4 个独立连接正确分片订阅
- [ ] 1m O-C 波动率在 1m K线消息内计算完成
- [ ] 15m Close-Close 波动率正确计算 (内部合成)
- [ ] Depth 按需订阅/退订正常工作
- [ ] Redis 正确存储品种规则 (exchangeInfo Hash)
- [ ] 1m K线正确写入 Redis (320根/品种)
- [ ] 15m K线正确写入 Redis (100根/品种)
- [ ] 1d K线正确写入 Redis (100根/品种)
- [ ] 断线重连指数退避正常
- [ ] 初始化阶段禁用告警,运行阶段60秒告警
- [ ] 单元测试覆盖核心组件
